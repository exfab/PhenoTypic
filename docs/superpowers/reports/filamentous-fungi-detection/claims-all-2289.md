# Recovered research claims (from completed fetch+verify; synthesis stages never ran)


## Astronomy: streaks, cosmic-web & ISM filaments  (4 confirmed / 90 total)

- ✅ **LSD is parameter-free and training-free: it works on any digital image without parameter tuning, requiring no labeled data or per-image threshold tuning.**  
  "It is designed to work on any digital image without parameter tuning."  
  — LSD: a Line Segment Detector (Grompone von Gioi et al., IPOL 2012) (URL: https://www.ipol.im/pub/art/2012/gjmr-lsd/article.pdf) [central,3v]
- ✅ **LSD controls its own false-detection rate via an a-contrario (Desolneux-Moisan-Morel) validation, allowing on average one false alarm per image — an automatic statistical threshold rather than a hand-tuned one, directly analogous to the a-contrario/FDR low-SNR thresholds called for in the brief.**  
  "It controls its own number of false detections: On average, one false alarm is allowed per image."  
  — LSD: a Line Segment Detector (Grompone von Gioi et al., IPOL 2012) (URL: https://www.ipol.im/pub/art/2012/gjmr-lsd/article.pdf) [central,3v]
- ✅ **The method uses a matched filter modeling coronal loops as a Gaussian cross-section profile (h(x,y) = 1/(sqrt(2pi)sigma) exp(-x^2/(2sigma^2)) for |y| <= L/2), rotated across 0-180 degrees, selecting the maximum response per pixel — i.e. it integrates evidence along a hypothesized oriented line and maximizes SNR (matched-filter / oriented directivity principle).**  
  "Io(x,y) = max{Iin(x,y) * h̃ᵢ(u,v)} i = 1,2,...,N ... the match filter requires that its output SNR should be maximal at time t₀"  
  — Automatic detection and extraction of coronal loops based on matched filter and oriented directivity (MNRAS 2019) (URL: https://academic.oup.com/mnras/article/490/4/5567/5625861) [central,3v]
- ✅ **The method is unsupervised and training-free, relying on prior knowledge of loop characteristics (Gaussian profile, curvature, width range) rather than learned parameters — directly relevant to the no-large-labeled-corpus constraint.**  
  "Most coronal loops can be considered as curved lines with a certain width"  
  — Automatic detection and extraction of coronal loops based on matched filter and oriented directivity (MNRAS 2019) (URL: https://academic.oup.com/mnras/article/490/4/5567/5625861) [central,3v]
- · **The review taxonomizes filament (curvilinear ridge) detectors into four families directly portable to hyphae detection: local differential operators (Hessian/gradient), template/model-matching (linear convolution, Hough-type, geometric multi-scale), topological/skeleton methods (DisPerSE, FilFinder, getsf), and machine-learning methods — giving an organizing frame for ranking ridge detectors by output type and SNR behavior.**  
  "One class of filament detection methods is built upon differential properties of the scalar field... Examples include Hessian-based extraction methods... A second major class of filament identification methods is based on matching the obser"  
  — Interstellar Filament Detection and Characterization: Methods and Implications (Galaxies 2026, review) (URL: https://doi.org/10.3390/galaxies14030053) [central,0v]
- · **Hessian eigenvalue (Frangi/Sato-family) ridge detection is intrinsically local and explicitly scale-dependent, and in practice highlights only the brightest, most prominent ridges — capturing few structures and missing faint filaments — confirming the baseline-brief concern that single-orientation Hessian models under-detect low-SNR thin structure.**  
  "In Hessian-based approaches, filaments are associated with anisotropic curvature: one eigenvalue of [the Hessian] is significantly negative, corresponding to the direction perpendicular to the filament, while the second eigenvalue is relati"  
  — Interstellar Filament Detection and Characterization: Methods and Implications (Galaxies 2026, review) (URL: https://doi.org/10.3390/galaxies14030053) [central,0v]
- · **The Rolling Hough Transform integrates oriented evidence in a local sliding window (orientation accumulation) and detects BOTH bright and faint elongated structures while emitting a per-pixel position-angle field — making it a strong low-SNR, orientation-aware candidate to add alongside or beat the phase-congruency baseline.**  
  "RHT uses a sliding window centered at each pixel... and evaluates the degree to which pixels are aligned along a given direction... RHT detects both bright and faint elongated structures resulting in many short or long quasi-straight struct"  
  — Interstellar Filament Detection and Characterization: Methods and Implications (Galaxies 2026, review) (URL: https://doi.org/10.3390/galaxies14030053) [central,0v]
- · **Topological/skeleton methods (DisPerSE via discrete Morse theory + persistence; FilFinder/getsf) are connectivity-first: DisPerSE traces a larger faint-and-extended filament NETWORK and these methods explicitly identify junction points and reconstruct connectivity — the property the brief prioritizes for downstream length/branching metrics — but their native output is a 1D spine/network, NOT a continuous binary mask, requiring dilation to recover width.**  
  "DisPerSE... identifies maxima and saddle points in the map and traces integral lines between them to reconstruct the underlying structures. A persistence criterion is then applied to remove features that may be arising from noise... DisPerS"  
  — Interstellar Filament Detection and Characterization: Methods and Implications (Galaxies 2026, review) (URL: https://doi.org/10.3390/galaxies14030053) [central,0v]
- · **A small-data / transfer learned option is demonstrated: a U-Net trained on masks derived from a Hessian-based detector reproduces curvature-based binary segmentation, and a separate U-Net trained on SYNTHETIC pixel-wise orientation maps predicts an orientation field directly — showing labels can be bootstrapped from a classical detector or synthetic data rather than a large hand-labeled corpus.**  
  "In one of the methods, U-Net architecture was used to perform binary filament segmentation, training the network on masks derived from a Hessian-based detector. In this case, the model learns to reproduce curvature-based detection and provi"  
  — Interstellar Filament Detection and Characterization: Methods and Implications (Galaxies 2026, review) (URL: https://doi.org/10.3390/galaxies14030053) [central,0v]
- · **LSD uses an a-contrario (Helmholtz-principle) validation that controls its own number of false detections, allowing on average one false alarm per image and requiring no parameter tuning to operate on any digital image.**  
  "LSD is a linear-time Line Segment Detector giving subpixel accurate results. It is designed to work on any digital image without parameter tuning. It controls its own number of false detections: on average, one false alarm is allowed per im"  
  — LSD: a Line Segment Detector (a-contrario / Helmholtz false-alarm control) (DOI: 10.5201/ipol.2012.gjmr-lsd) [central,0v]
- · **The single detection threshold epsilon is fixed at 1 once and for all, results are not sensitive to its value (the detection limit varies only like sqrt(-log epsilon)), so any reasonable setting gives very similar results.**  
  "Following Desolneux, Moisan, and Morel [3, 4], we set ε = 1 once for all. This corresponds to accepting on average one false detection per image in the a contrario model, which is reasonable. Also, the detection result is not sensitive to t"  
  — LSD: a Line Segment Detector (a-contrario / Helmholtz false-alarm control) (DOI: 10.5201/ipol.2012.gjmr-lsd) [central,0v]
- · **Detection is driven by local edge/ridge orientation, not intensity thresholding: LSD computes a per-pixel level-line angle field and segments it into connected regions sharing the same orientation up to a tolerance tau, which become candidate line-support regions.**  
  "The algorithm starts by computing the level-line angle at each pixel to produce a level-line field, i.e., a unit vector field such that all vectors are tangent to the level line going through their base point. Then, this field is segmented "  
  — LSD: a Line Segment Detector (a-contrario / Helmholtz false-alarm control) (DOI: 10.5201/ipol.2012.gjmr-lsd) [central,0v]
- · **LSD only detects locally-straight structures; for curved structures it produces a polygonal approximation of short straight segments, and it fails outright when curvature is too strong (it does not model junctions or smooth curvilinear networks).**  
  "Note that when curves are present, LSD produces short line segments corresponding to curve sections that are locally straight. The result is a polygonal approximation for curves. When the curvature is too strong LSD of course fails."  
  — LSD: a Line Segment Detector (a-contrario / Helmholtz false-alarm control) (DOI: 10.5201/ipol.2012.gjmr-lsd) [central,0v]
- · **OCCULT-2 preprocesses images with a bandpass (unsharp-masking) filter: a lowpass boxcar of width nsm1 suppresses photon/Poisson data noise while a highpass boxcar of width nsm2 enhances fine structure, with the combination defined as smooth(I,nsm1) - smooth(I,nsm2). This is directly analogous to PhenoTypic's large-sigma Gaussian background subtraction and provides a noise-suppression + ridge-enhancement front end with the filter widths bracketing the target structure cross-section.**  
  "A lowpass filter with a boxcar smoothing constant nsm1 smoothes out the data noise (e.g., photon noise with Poisson statistics in astrophysical and microscopy images...), while a highpass filter with a boxcar smoothing constant nsm2 enhance"  
  — A Code for Automated Tracing of Coronal Loops Approaching Visual Perception (OCCULT-2) (DOI: 10.1007/s11207-013-0269-9) [central,0v]
- · **Tracing is an explicit ridge-following tracker, not a per-pixel threshold: it starts at the global flux maximum, traces bidirectionally to both endpoints, and at each step determines the local ridge orientation (azimuthal angle) and local curvature radius, extrapolating one pixel via a second-order polynomial. The guiding criterion sums/averages flux ALONG a hypothesized constant-curvature ridge segment (maximal along the ridge, minimal perpendicular) — exactly the integrate-evidence-along-a-line strategy favored for low-SNR faint filaments.**  
  "The step-wise tracing along a loop structure position si is carried out by determining first the direction of the local ridge (defined by the azimuthal angle αl with respect to the x-axis), and secondly by determining the local curvature ra"  
  — A Code for Automated Tracing of Coronal Loops Approaching Visual Perception (OCCULT-2) (DOI: 10.1007/s11207-013-0269-9) [central,0v]
- · **OCCULT-2 bridges small intensity gaps during tracing by tolerating a few consecutive sub-threshold pixels (ngap≈3) before declaring a loop endpoint, allowing reliable tracing of faint/noisy structures through dropouts — a built-in gap-bridging mechanism analogous in intent to the PhenoTypic Dijkstra reconnection but operating inline during tracing rather than as a separate cost-surface pathfinding step.**  
  "The first endpoint generally is demarcated at a location where the bandpass-filtered image has zero or negative values, so one could just detect the first image pixel along the guiding segment sk that is non-positive. However, in order to a"  
  — A Code for Automated Tracing of Coronal Loops Approaching Visual Perception (OCCULT-2) (DOI: 10.1007/s11207-013-0269-9) [central,0v]

## Biomedical analogs & curvilinear tracking  (4 confirmed / 95 total)

- ✅ **A Hessian-based enhancement embedding module (using second-order-gradient eigenvalues lambda_1, lambda_2) improves the contrast of low-contrast thin guidewires before segmentation, integrating classical Hessian ridge enhancement into a deep network.**  
  "Hessian-based enhancement embedding module ... designed to improve contrast of the guidewire ... Uses eigenvalue features (λ₁, λ₂) computed from second-order gradients for enhancement"  
  — Real-time guidewire tracking and segmentation in intraoperative X-ray (URL: https://arxiv.org/html/2404.08805v1) [central,3v]
- ✅ **A detector outputs a bounding box first to crop the structure, which reduces the foreground/background class imbalance of the thin-structure segmentation task.**  
  "a detector is deployed first to output the bounding box...which could reduce the class imbalance of the segmentation task"  
  — Real-time guidewire tracking and segmentation in intraoperative X-ray (URL: https://arxiv.org/html/2404.08805v1) [central,3v]
- ✅ **The Hessian-enhanced network (HessianNet) outperforms a baseline U-Net on thin-structure segmentation, achieving Dice 0.8990 versus U-Net's 0.8890.**  
  "Achieves Dice score of 0.8990 vs. baseline U-Net's 0.8890"  
  — Real-time guidewire tracking and segmentation in intraoperative X-ray (URL: https://arxiv.org/html/2404.08805v1) [central,3v]
- ✅ **The catheter tip tracking pipeline frames the thin-structure detection task as deep-learning semantic SEGMENTATION (producing a per-pixel mask) rather than direct point/bbox tracking, comparing U-Net, U-Net+Transformer, and SegFormer architectures in both two-class and three-class formulations — transferable as a learned, mask-producing alternative to the classical phase-congruency baseline for curvilinear structures.**  
  "The team trained and compared three deep learning segmentation architectures (U-Net, U-Net+Transformer, and SegFormer) using both two-class and three-class formulations, followed by component filtering and contour-based path tracking."  
  — Toward real-time autonomous navigation: transformer-based catheter tip tracking in fluoroscopy (URL: https://link.springer.com/article/10.1007/s11548-026-03647-7) [central,3v]
- · **A learned adversarial 'error discrimination network' (a discriminator that judges whether the segmentation network's per-pixel vessel predictions are correct, with the segmenter trained to minimize the discriminator's detected errors) improves thin-vessel sensitivity and vessel connectivity over plain per-pixel segmentation — a topology-targeting alternative to standard per-pixel losses.**  
  "The researchers developed an error discrimination network to improve automated retinal vessel segmentation, particularly for challenging features like thin vessels. Their approach trains a discrimination network to identify incorrect predic"  
  — Improving sensitivity and connectivity of retinal vessel segmentation via error discrimination network (DOI: 10.1002/mp.15627) [central,0v]
- · **The discriminator is trained on three sample types — vessel ground truth, vessel segmented by the network, and synthetic 'artificial thin vessel error samples' — where the artificially generated thin-vessel errors specifically raise sensitivity to missed/wrong small vessels; this is a label-augmentation strategy usable without a large hand-labeled corpus of thin structures.**  
  "Three types of vessel samples and corresponding error masks are used to train D: (1) vessel ground truth; (2) vessel segmented by S; (3) artificial thin vessel error samples that further improve the sensitivity of D to wrong small vessels."  
  — Improving sensitivity and connectivity of retinal vessel segmentation via error discrimination network (DOI: 10.1002/mp.15627) [central,0v]
- · **The method preserves network connectivity, as measured by three explicit topology-relevant metrics (Conn, Inf, Cor) on which it scores highest — directly demonstrating a junction/gap-aware (connectivity-prioritized) segmentation result rather than only pixel accuracy.**  
  "The top scores of the three topology-relevant metrics (Conn, Inf, and Cor) demonstrate that the vessels extracted by the method have excellent connectivity."  
  — Improving sensitivity and connectivity of retinal vessel segmentation via error discrimination network (DOI: 10.1002/mp.15627) [central,0v]
- · **The method addresses thin-vessel segmentation and vessel connectivity directly, framing both as the central weaknesses of existing deep-learning curvilinear segmenters — precisely the low-SNR thin-structure and topology/gap regimes in the brief.**  
  "current approaches are still inadequate in challenging vessels (e.g., thin vessels) and rarely focus on the connectivity of vessel segmentation."  
  — Improving sensitivity and connectivity of retinal vessel segmentation via error discrimination network (DOI: 10.1002/mp.15627) [central,0v]
- · **An auxiliary 'error discrimination network' (D) is trained to predict where the segmentation network (S) is wrong, and S is optimized to reduce D's error predictions — a GAN-like but distinct adversarial scheme that acts as a topology/difficult-vessel-aware loss rather than a per-pixel loss.**  
  "We propose using an error discrimination network (D) to distinguish whether the vessel pixel predictions of the segmentation network (S) are correct, and S is trained to obtain fewer error predictions of D. Our method is similar to, but not"  
  — Improving sensitivity and connectivity of retinal vessel segmentation via error discrimination network (DOI: 10.1002/mp.15627) [central,0v]
- · **Synthetic/artificial thin-vessel error samples are deliberately injected into D's training set specifically to raise sensitivity to wrong small (thin) vessels — a data-augmentation strategy to combat severe foreground/background imbalance and thin-structure dropout without a large labeled corpus.**  
  "Three types of vessel samples and corresponding error masks are used to train D, as follows: (1) vessel ground truth; (2) vessel segmented by S; (3) artificial thin vessel error samples that further improve the sensitivity of D to wrong sma"  
  — Improving sensitivity and connectivity of retinal vessel segmentation via error discrimination network (DOI: 10.1002/mp.15627) [central,0v]
- · **A lightweight U-Net trained mostly on synthetic data (9000 synthetic fluoroscopic images) and fine-tuned on only a small number of real annotated frames (240/493/579) produces a per-pixel binary catheter/guidewire mask, directly addressing the small-label / no-large-corpus constraint via transfer learning.**  
  "We employ Convolutional Neural Networks (CNNs) and propose a transfer learning approach, using synthetic fluoroscopic images, to develop a lightweight version of the U-Net architecture. Our strategy, requiring a small amount of manually ann"  
  — Catheter segmentation in X-ray fluoroscopy using synthetic data and transfer learning with light U-nets (DOI: 10.1016/j.cmpb.2020.105420) [central,0v]
- · **Without the synthetic-data pretraining, the small real dataset alone is insufficient: a leave-one-out cross-validation trained only on the real sequences fails to learn the task (Dice ~0.1), demonstrating that thin-structure segmentation networks need a large training corpus that synthetic generation can supply.**  
  "Due to the limited number of training data, the resulting network is not able to effectively learn the catheter segmentation task (Dice coefficient  ~ 0.1)."  
  — Catheter segmentation in X-ray fluoroscopy using synthetic data and transfer learning with light U-nets (DOI: 10.1016/j.cmpb.2020.105420) [central,0v]
- · **Loss-function choice is decisive for thin-structure / class-imbalanced segmentation: a Dice-overlap loss dramatically outperforms categorical cross-entropy after fine-tuning (validation accuracy ~0.58/0.61 vs ~0.14/0.02), and both losses were reweighted 1/10 background-to-foreground to counter the under-represented catheter class.**  
  "in the fine-tuning phase, the cross-entropy loss function showed a dramatic drop in performance, with an accuracy on the validation set of about 0.14 (0.02) in(), as opposed to the 0.58 (0.61) achieved with the Dice overlap metric. These re"  
  — Catheter segmentation in X-ray fluoroscopy using synthetic data and transfer learning with light U-nets (DOI: 10.1016/j.cmpb.2020.105420) [central,0v]
- · **VNR is a purely algorithmic (training-free) post-processing reconnection pipeline that takes a deep-learning vessel segmentation as input and outputs a single-connected-component mask, making it a direct classical-CV alternative to PhenoTypic's Dijkstra reconnection step rather than a learned method.**  
  "we developed a post-processing pipeline to ensure a connected structure for the retinal vessels networks. The proposed pipeline named VNR for Vessels Network Retrieval, generates segmentations with a single connected component (CC). This is"  
  — Ensuring a connected structure for Retinal Vessels Deep-Learning Segmentation (ICCV 2023 W/CVAMD) (URL: https://openaccess.thecvf.com/content/ICCV2023W/CVAMD/papers/Dulau_Ensuring_a_Connected_Structure_for_Retinal_Vessels_Deep-Learning_Segmentation_ICCVW_2023_paper.pdf) [central,0v]
- · **Restoring network connectivity gives essentially zero per-pixel accuracy cost while collapsing fragmentation from 174 components to 1: Dice held at 0.839 to 0.840 while connected components dropped from 174 to 1, demonstrating that gap-bridging can preserve pixel accuracy.**  
  "Before applying VNR the predicted segmentations had an average Dice score of 0.839 with 174 CCs. As a result, 173 CCs need to be deleted or reconnected. After applying VNR, the segmentations have an average Dice score of 0.840 with only 1 C"  
  — Ensuring a connected structure for Retinal Vessels Deep-Learning Segmentation (ICCV 2023 W/CVAMD) (URL: https://openaccess.thecvf.com/content/ICCV2023W/CVAMD/papers/Dulau_Ensuring_a_Connected_Structure_for_Retinal_Vessels_Deep-Learning_Segmentation_ICCVW_2023_paper.pdf) [central,0v]
- · **Adding a clDice topology-preserving loss term (in a BCE+Dice+clDice hybrid) improves segmentation continuity of thin tubular structures and reduces breaks/discontinuities, beating BCE+Dice alone on Macro-F1, Dice, MIoU and OIoU (though it slightly lowered AUC). This validates topology-aware/centerline-aware losses (clDice) as a gap/junction-preserving mechanism for thin-structure segmentation, directly transferable to hyphae network connectivity.**  
  "To achieve more accurate segmentation continuity of the guidewire morphology and to minimize issues of discontinuity and breakage in the segmentation results, we employ the clDice. The clDice is a topology-preserving similarity metric speci"  
  — Lightweight attention network for guidewire segmentation and localization in clinical fluoroscopic images of vascular interventional surgery (DOI: 10.21037/qims-2024-2926) [central,0v]

## Cryo-EM low-SNR filament picking  (4 confirmed / 93 total)

- ✅ **STRIPER is a training-free classical line detector: it enhances filaments with oriented Gaussian smoothing kernels (taking the max response over rotated kernels) and then extracts lines with Steger's (1998) ridge-detection algorithm using hysteresis thresholds — directly transferable as a Steger + oriented-filter alternative to phase congruency.**  
  "Instead of using a trained deep neural network to identify the boxes along the filaments, the STRIPER filament-picking procedure is based on a classical line-detection approach... the filaments are enhanced using the same oriented Gaussian "  
  — Two particle picking procedures for filamentous proteins: SPHIRE-crYOLO filament mode and SPHIRE-STRIPER (URL: https://www.biorxiv.org/content/10.1101/2020.02.28.969196v1.full) [central,3v]
- ✅ **Steger's ridge detector explicitly handles junctions/crossings: it detects line crossings, marks the meeting point as a junction and splits the line in two, and STRIPER then splits detected lines at crossing points — a junction-preserving behavior contrasting with Frangi/Sato vesselness which suppresses junctions.**  
  "Repeat (b) until (i) no valid line point is found, thus indicating the end of the line, or (ii) the selected line point is already part of a different line. Mark this point as a junction and split the line in two... After extracting the lin"  
  — Two particle picking procedures for filamentous proteins: SPHIRE-crYOLO filament mode and SPHIRE-STRIPER (URL: https://www.biorxiv.org/content/10.1101/2020.02.28.969196v1.full) [central,3v]
- ✅ **Low SNR breaks naive line detection, so STRIPER adds an oriented-Gaussian line-enhancing preprocessing step specifically to integrate evidence along oriented lines before ridge detection — confirming the brief's claim that low-SNR favors orientation-integrating filters over per-pixel decisions.**  
  "Cryo-EM images typically have a very low signal-to-noise ratio, which is problematic for line-detection algorithms. We therefore included a line-enhancing pre-processing step in STRIPER."  
  — Two particle picking procedures for filamentous proteins: SPHIRE-crYOLO filament mode and SPHIRE-STRIPER (URL: https://www.biorxiv.org/content/10.1101/2020.02.28.969196v1.full) [central,3v]
- ✅ **The recall/precision operating point is controllable through a small, tunable parameter set: STRIPER exposes four parameters (filament width, mask width, upper and lower hysteresis thresholds) and provides a grid-search optimization to estimate the thresholds from 2-3 manually annotated micrographs — a concrete single-knob-with-default design analog.**  
  "To run the STRIPER filament procedure, four parameters need to be provided: (i) the filament width in pixels... (ii) the mask width in pixels, which is set to 100 by default... and (iii) the upper and (iv) the lower threshold used as hyster"  
  — Two particle picking procedures for filamentous proteins: SPHIRE-crYOLO filament mode and SPHIRE-STRIPER (URL: https://www.biorxiv.org/content/10.1101/2020.02.28.969196v1.full) [central,2v]
- · **FibrilFinder detects thin filaments by computing the 2D Hessian matrix per pixel and using its eigenvalues as a normalized ridge-detection output, exploiting that fibrils are elongated, nearly-linear objects of approximately known width darker than background.**  
  "the second derivatives are calculated to derive the 2D Hessian matrix at each pixel. Finally, the eigenvalues of the Hessian matrix are used to calculate the normalized output value."  
  — Automated picking of amyloid fibrils from cryo-EM images for helical reconstruction with RELION (FibrilFinder/FibrilFixer) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC8217313/) [central,0v]
- · **The method bridges low-SNR regions by integrating evidence along a hypothesized line via a long-rectangle (non-local) convolution rather than per-pixel decisions, the core principle the brief favors for the low-SNR regime.**  
  "The non-local nature of the long rectangle convolution step helps to bridge the low signal-to-noise ratio region."  
  — Automated picking of amyloid fibrils from cryo-EM images for helical reconstruction with RELION (FibrilFinder/FibrilFixer) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC8217313/) [central,0v]
- · **FibrilFinder is fully unsupervised/training-free and runs across different samples without parameter changes, demonstrating a training-free classical-CV ridge detector generalizing across datasets.**  
  "we have designed our algorithm to run on multiple samples without requiring changes in the parameters."  
  — Automated picking of amyloid fibrils from cryo-EM images for helical reconstruction with RELION (FibrilFinder/FibrilFixer) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC8217313/) [central,0v]
- · **A CNN applied directly to the raw low-SNR cryo-EM images fails (only ~20% crossover-detection precision and 19% entirely-labelled-fibril precision), whereas the hybrid classical-CV-preprocessing + U-Net pipeline reaches ~91.5% precision; learned methods alone cannot handle low-quality, weakly-labelled filament data.**  
  "even when considering only crossovers on actual fibrils for which hand-labelled crossovers exist, the precision of the crossover detection using solely a neural network comparable to the network considered in our method is only about 20%. ."  
  — Automatic identification of crossovers in cryo-EM images of amyloid fibrils with machine learning (Weber et al., J. Microscopy 2020) (URL: https://onlinelibrary.wiley.com/doi/full/10.1111/jmi.12858) [central,0v]
- · **Filaments are extracted not by intensity thresholding but by convolving the image with straight-line kernels at 60 different orientations spanning [0,pi] and taking the per-orientation response, which highlights line segments oriented accordingly — a training-free oriented-filter-bank / Hough-style ridge detector.**  
  "We apply this method of object detection to our data using kernels which depict straight lines with 60 different orientations equally spaced in alpha in [0,pi]. For any fixed alpha, the convolution highlights fibril segments which are orien"  
  — Automatic identification of crossovers in cryo-EM images of amyloid fibrils with machine learning (Weber et al., J. Microscopy 2020) (URL: https://onlinelibrary.wiley.com/doi/full/10.1111/jmi.12858) [central,0v]
- · **The oriented-convolution line-detection step is explicitly robust to noise, to minor shape deviations, and to partial occlusion by overlapping fibrils — the specific failure modes (low SNR, dense overlap) targeted by the brief.**  
  "this method of object detection is robust with respect to noise and minor deviations in the object's shape and still works when the object is partly covered by other objects. This is important for application to our problem as fibrils can o"  
  — Automatic identification of crossovers in cryo-EM images of amyloid fibrils with machine learning (Weber et al., J. Microscopy 2020) (URL: https://onlinelibrary.wiley.com/doi/full/10.1111/jmi.12858) [central,0v]
- · **crYOLO achieves robust low-SNR detection: on simulated data the area-under-curve (AUC) detection quality stays >0.8 up to noise level 6 and >0.6 at noise level 8, where particles are visually barely distinguishable from background (SNR as low as ~0.023-0.014) - demonstrating a single-pass CNN detector can integrate full-image context to find targets near the noise floor.**  
  "Up to a noise level of 6 the AUC value is >0.8 and even for a noise level of 8 the AUC values stay >0.6. In images with a noise level of ≥8 the particles are visually barely distinguishable from the background ... The corresponding signal t"  
  — SPHIRE-crYOLO is a fast and accurate fully automated particle picker for cryo-EM (DOI: 10.1038/s42003-019-0437-z) [central,0v]
- · **The method requires only small, positive-only annotation: as few as 40 particles from a single micrograph pair yielded a high AUC of 0.9, and no negative examples (background, contamination) need to be labeled because all unlabeled positions are treated as negatives - directly addressing the brief's small-set-annotation constraint.**  
  "Even if the model was trained with as few as 40 KLH particles from 1 micrograph pair, crYOLO still demonstrated a high AUC value of 0.9 ... the picking of negative examples (including background, carbon edges, ice contamination, and bad par"  
  — SPHIRE-crYOLO is a fast and accurate fully automated particle picker for cryo-EM (DOI: 10.1038/s42003-019-0437-z) [central,0v]
- · **crYOLO is a deep-learning particle picker (YOLO object-detection framework) that automatically picks particles in cryo-EM micrographs with low signal-to-noise ratio with minimal human supervision, and it processes up to six micrographs per second — establishing a learned single-pass detector as viable in the hardest low-SNR regime.**  
  "It is based on a deep neural network and the You Only Look Once (YOLO) object detection framework. This approach enables the automated picking of particles within cryo-EM micrographs with a low signal-to-noise ratio requiring minimal human "  
  — The evolution of SPHIRE-crYOLO particle picking and its application in automated cryo-EM processing workflows (DOI: 10.1038/s42003-020-0790-y) [central,0v]
- · **crYOLO's filament mode does not segment filaments directly; it first detects particles as support points and then, in a post-processing step, traces the filaments and places boxes along them at a user-defined spacing — i.e., point-detection plus an explicit tracing/linking stage rather than a per-pixel binary mask, with the tracing distance as a tunable knob.**  
  "The new filament picking procedure initially follows the general workflow of crYOLO. In a post-processing step, it uses the picked particles as support points to trace the filaments. The boxes are then placed along the filaments in a distan"  
  — The evolution of SPHIRE-crYOLO particle picking and its application in automated cryo-EM processing workflows (DOI: 10.1038/s42003-020-0790-y) [central,0v]
- · **By design the filament tracer selects only single filaments and deliberately omits positions where filaments cross or overlap — i.e., it avoids junctions/crossings rather than preserving them, which is the opposite of the brief's junction-preserving requirement.**  
  "Optimally, only single filaments are selected and positions where filaments cross or overlap are omitted."  
  — The evolution of SPHIRE-crYOLO particle picking and its application in automated cryo-EM processing workflows (DOI: 10.1038/s42003-020-0790-y) [central,0v]
- · **crYOLO supports fine-tuning by retraining only the last few network layers while freezing earlier layers, which substantially reduces the amount of training data needed to adapt a working model and also speeds up training and lowers GPU memory consumption — directly supporting the small-data transfer/fine-tune constraint.**  
  "In this case, only the last few layers of the network are retrained while previous layers are fixed. This effectively reduces the amount of training data needed to improve a working model. A major advantage of this approach is a substantial"  
  — The evolution of SPHIRE-crYOLO particle picking and its application in automated cryo-EM processing workflows (DOI: 10.1038/s42003-020-0790-y) [central,0v]

## Cytoskeleton filament extraction (closest twin)  (3 confirmed / 87 total)

- ✅ **SOAX's core method is multiple Stretching Open Active Contours (SOACs) — parametric active-contour curves that are seeded on intensity ridges, evolve toward filament centerlines under combined image and stretching forces, and stop at filament tips. This is the canonical 'stretching open snake' technique for curvilinear-structure extraction, directly transferable to fungal hyphae as a centerline-tracing alternative to threshold/Hessian methods.**  
  "The underlying method of SOAX is the multiple Stretching Open Active Contours (SOACs) method that was proposed to extract the 3D meshwork of actin filaments imaged by confocal microscopy... A SOAC is a parametric curve that “evolves”: it is"  
  — SOAX: A software for quantification of 2D/3D biopolymer networks (Scientific Reports) (URL: https://www.nature.com/articles/srep09081) [central,3v]
- ✅ **SOAX explicitly forms and configures junctions: SOAC tips that collide with another SOAC body form T-junctions, nearby T-junctions are clustered into single junctions, and SOACs are cut/spliced so they do not end or bend sharply at junctions — i.e. junctions/crossings are first-class outputs that are preserved, the opposite of Frangi/Sato vesselness which suppresses junctions.**  
  "SOACs stop stretching at a filament end or when their tip collides with the body of another SOAC to form a T-junction... The final stage... is clustering nearby T-junctions into a single junction followed by configuring the local connectivi"  
  — SOAX: A software for quantification of 2D/3D biopolymer networks (Scientific Reports) (URL: https://www.nature.com/articles/srep09081) [central,3v]
- ✅ **SOAX is positioned specifically for low/inhomogeneous-SNR images where thresholding+thinning of a tubularity map fails; integrating evidence along an evolving contour is robust to noise, but the trade-off is that its two key parameters (ridge threshold tau and stretch factor) must be tuned to the image SNR — a controllable but not parameter-free sensitivity knob.**  
  "Thresholding results, however, can suffer from inhomogeneous signal-to-noise ratio (SNR)... While the SOAX method is robust against noise, its parameters need to be adjusted depending on the type of biopolymer and the image SNR... network e"  
  — SOAX: A software for quantification of 2D/3D biopolymer networks (Scientific Reports) (URL: https://www.nature.com/articles/srep09081) [central,3v]
- · **Stretching Open Active Contours (SOACs) are initialized at ridge points detected via a sign change in the image gradient (a ridge-geometry detector, not an intensity threshold), and grow by applying a stretching force only at contour endpoints whose magnitude is proportional to local image intensity, evolving by energy minimization until length/position stabilize.**  
  "The force direction is along the tangential direction at the ends and its magnitude is proportional to the image intensity"  
  — Extraction and Analysis of Actin Networks Based on Open Active Contour Models (Xu et al., PMC3150558) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC3150558/) [central,0v]
- · **The method explicitly handles intersecting and (to some extent) overlapping filaments and overcomes connectivity loss from intensity gaps or faint filaments via simultaneous multi-SOAC evolution -- directly addressing the junction-preserving / gap-bridging requirement where naive Hessian vesselness fails.**  
  "handles well intersecting filaments and, to some extent, overlapping filaments"  
  — Extraction and Analysis of Actin Networks Based on Open Active Contour Models (Xu et al., PMC3150558) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC3150558/) [central,0v]
- · **Junctions are detected and SOACs merged using geometric criteria: junction points are SOAC points neighboring another SOAC's points, and SOAC ends merge when aligned with orientation above an angle threshold of 2pi/3 -- an explicit orientation-coherence-based reconnection rule (an alternative cost/merge surface to Dijkstra least-cost pathfinding).**  
  "Junction points are those SOAC points having one or more neighboring SOAC points that belong to another SOAC"  
  — Extraction and Analysis of Actin Networks Based on Open Active Contour Models (Xu et al., PMC3150558) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC3150558/) [central,0v]
- · **SOAX/TSOAX requires a local signal-to-noise ratio of order 5 for reliable automated filament extraction, indicating limited robustness in the low-SNR regime where faint filaments approach the noise floor.**  
  "a value of a signal to noise ratio (defined in the local neighborhood of snake point) of order 5 is needed for reliable automated extraction in SOAX."  
  — Automated Tracking of Biopolymer Growth and Network Deformation with TSOAX (Scientific Reports / PMC6368602) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC6368602/) [central,0v]
- · **The active-contour approach explicitly detects junctions by dissecting curves at colliding points ('T-junctions') and clustering them into network junctions, directly addressing the junction-preservation requirement that Hessian vesselness fails.**  
  "These curves are dissected at colliding points ('T-junctions') which are clustered into detected network junctions."  
  — Automated Tracking of Biopolymer Growth and Network Deformation with TSOAX (Scientific Reports / PMC6368602) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC6368602/) [central,0v]
- · **Stretching open active contours can track filaments through intersection/crossing points, handling overlapping structures that cause dropout in single-orientation models.**  
  "The program successfully tracked many filaments as they grew past intersection points with other filaments."  
  — Automated Tracking of Biopolymer Growth and Network Deformation with TSOAX (Scientific Reports / PMC6368602) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC6368602/) [central,0v]
- · **The method outputs centerline (open-curve) representations of the network rather than a clean per-pixel binary mask, so a binary mask would require an additional rasterization/dilation step.**  
  "the centerlines of a network are extracted frame by frame using multiple open curves...which are automatically initialized and elongated sequentially."  
  — Automated Tracking of Biopolymer Growth and Network Deformation with TSOAX (Scientific Reports / PMC6368602) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC6368602/) [central,0v]
- · **SOACs (stretching open active contours) represent filaments as continuous parametric curves that preserve topology at intersections and can grow over faint/low-contrast elements, whereas pixel-wise (threshold/skeletonization/level-set/MRF) methods require a separate, noise-fragile curve-reconstruction step. This makes SOACs a centerline/curve extractor rather than a direct binary-mask producer, with explicit junction-preservation and gap-bridging behavior.**  
  "Compared to other implementations that segment linear structures, the SOACs have the advantage of using parametric curves of fixed topology to represent filaments, and they are particularly good at preserving topology at intersections and g"  
  — Segmentation and tracking of cytoskeletal filaments using open active contours (DOI: 10.1002/cm.20481) [central,0v]
- · **The external image energy is built from a Gaussian-smoothed image (not the gradient-magnitude term used in conventional segmentation), and its gradient vectors point toward the filament center, so the contour is attracted to the central bright ridge of the filament — exploiting local ridge geometry rather than an intensity threshold.**  
  "We use a Gaussian-filtered image [...] as the image term [...] This term is different from the gradient magnitude term [...] commonly used in conventional segmentation methods []. As shown in, the gradient vectors corresponding to [the imag"  
  — Segmentation and tracking of cytoskeletal filaments using open active contours (DOI: 10.1002/cm.20481) [central,0v]
- · **In its presented form the method does not segment network structures and handles crossed/overlapping filaments only via ad hoc strategies (greater tip stiffness and tip 'jump'), indicating limited automatic handling of dense overlap/junction networks — a key limitation for dense mycelial networks.**  
  "While in its present form our method does not describe network structures, such an extension is possible. Strategies can also be introduced to handle crossed filaments—for instance, in, we proposed two strategies, greater tip stiffness and "  
  — Segmentation and tracking of cytoskeletal filaments using open active contours (DOI: 10.1002/cm.20481) [central,0v]
- · **TSOAX extracts filament network centerlines using stretching open active contours (SOAC), a training-free parametric active-contour method, not an intensity threshold or learned model.**  
  "an open source software tool that combines a global and local correspondence algorithm to track biopolymer networks in 2D and 3D, using stretching open active contours"  
  — Automated Tracking of Biopolymer Growth and Network Deformation with TSOAX (Scientific Reports / PMC6368602) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC6368602/) [central,0v]
- · **TSOAX/SOAC is junction-aware: it explicitly links dissected curves at junctions using a smoothness criterion (curves that form a smooth continuation through the junction are preferentially linked), directly addressing the junction-suppression failure of Hessian vesselness.**  
  "two dissected curves incident at a junction should have higher affinity (more likely to be linked) if 1) they form a smooth curve passing through the junction if linked; 2) their changes compared to previous frames are similar"  
  — Automated Tracking of Biopolymer Growth and Network Deformation with TSOAX (Scientific Reports / PMC6368602) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC6368602/) [central,0v]
- · **The method outputs parametric centerlines / open curves and junction coordinates (a vector representation), not a binary pixel mask, so producing a binary fungal mask would require an additional rasterization/dilation step.**  
  "centerlines of a network...using multiple open curves"  
  — Automated Tracking of Biopolymer Growth and Network Deformation with TSOAX (Scientific Reports / PMC6368602) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC6368602/) [central,0v]

## Diffusion preprocessing & chromatic-aberration rejection  (4 confirmed / 194 total)

- ✅ **Coherence-enhancing diffusion uses the structure tensor's eigenvectors/eigenvalues to build a diffusion tensor that preferentially smooths ALONG the dominant local orientation (the coherence direction), so it diffuses along curvilinear/flow-like structures rather than across them. This is the core orientation-aware preprocessing principle directly relevant to enhancing thin oriented hyphae before thresholding.**  
  "This coherence descriptor enables us to construct a diffusion tensor which steers the diffusion process in each channel in such a way that diffusion is encouraged along the preferred structure orientation."  
  — Coherence-enhancing diffusion of colour images (Weickert, Image & Vision Computing 1999) (URL: https://www.sciencedirect.com/science/article/abs/pii/S0262885698001024) [central,3v]
- ✅ **The method is explicitly designed to enhance one-dimensional (line-like) structures and to close interrupted lines in poor-quality flow-like images, i.e. it performs gap-bridging along oriented structures, the same connectivity problem central to reconnecting fragmented hyphae.**  
  "Some images containing flow-like patterns are of poor quality, such that it becomes necessary to enhance them by closing interrupted lines"  
  — Coherence-enhancing diffusion of colour images (Weickert, Image & Vision Computing 1999) (URL: https://www.sciencedirect.com/science/article/abs/pii/S0262885698001024) [central,3v]
- ✅ **Coherence-enhancing diffusion is an enhancement/restoration filter for flow-like, line-like structures that smooths anisotropically mainly ALONG the dominant local orientation (eigenvector of the structure tensor with the smallest eigenvalue), not a binary segmenter. This makes it a preprocessing step that strengthens hyphal ridges before thresholding, exactly the role the brief asks of coherence-enhancing diffusion as a confounder-robust preprocessor.**  
  "This scale-space and image restoration technique has been introduced in [40] for the enhancement of flow-like textures with line-like structures... apply a nonlinear diffusion process whose diffusion tensor allows anisotropic smoothing by a"  
  — Coherence-Enhancing Diffusion Filtering (Weickert, IJCV 1999) (URL: https://link.springer.com/article/10.1023/A:1008009714131) [central,3v]
- ✅ **A primary, explicitly stated design goal of coherence-enhancing diffusion is closing gaps in interrupted line-like structures by smoothing along the line direction — directly relevant to the dense-overlap/intensity-gap regime and as an alternative gap-bridging mechanism to Dijkstra reconnection.**  
  "When the goal consists e.g. of closing gaps in an interrupted line-like structure, it is clear that slight deviations from the correct smoothing direction will destroy any desired filter effect and result in a deterioration of the line by i"  
  — Coherence-Enhancing Diffusion Filtering (Weickert, IJCV 1999) (URL: https://link.springer.com/article/10.1023/A:1008009714131) [central,3v]
- · **The paper proposes correcting chromatic aberration computationally (via image processing) as an alternative to more expensive/complex optics, targeting light-weight cameras such as single-ball lenses. This places the method squarely in the chromatic-aberration / color-fringing rejection family relevant to the unhandled CA gap in the PhenoTypic baseline.**  
  "Instead of more expensive and complex optics, recent years, many researches are focused on high-quality photography using light-weight cameras, such as single-ball lens, with computational image processing."  
  — Chromatic Aberration Correction Using Cross-Channel Prior in Shearlet Domain (Springer, ACCV 2020) (URL: https://link.springer.com/chapter/10.1007/978-3-030-69532-3_7) [central,0v]
- · **The correction method is built on a cross-channel prior operating in the shearlet domain, i.e. it exploits a relationship between color channels and uses the shearlet transform (a directionally/anisotropically selective multiscale representation well suited to edges and elongated/curvilinear structures). This is directly the color/multichannel + oriented-transform combination called for as a CA-fringe-rejection candidate.**  
  "Chromatic Aberration Correction Using Cross-Channel Prior in Shearlet Domain"  
  — Chromatic Aberration Correction Using Cross-Channel Prior in Shearlet Domain (Springer, ACCV 2020) (URL: https://link.springer.com/chapter/10.1007/978-3-030-69532-3_7) [central,0v]
- · **Replacing the intensity-based structure tensor with a phase-congruency tensor (PCT) inside coherence-enhancing anisotropic diffusion makes the enhancement largely insensitive to intensity/contrast variation along a curvilinear structure, because phase congruency is contrast-independent — directly addressing the brief's 'ridge geometry not intensity threshold' requirement and the low-SNR/intensity-inhomogeneity regime.**  
  "Here we propose a contrast independent anisotropic diffusion filtering of curvilinear structures based on a novel concept: the Phase Congruency Tensor (PCT). The phase angle of the PCT determines the preferred diffusion direction and the di"  
  — Coherence Enhancing Diffusion Filtering based on the Phase Congruency Tensor (DOI: 10.1109/ISBI.2012.6235519) [central,0v]
- · **The PCT-based diffusion produces higher contrast-to-noise ratios than the intensity-based coherence measure across increasing noise levels, demonstrating robustness in the low-SNR regime; e.g. at noise variance 0.4 the PCT vesselness (Vpc) CNR is 51.96 versus 45.66 for the intensity-based (mu1-mu2)^2 measure and 22.79 for the raw image.**  
  "In addition, a contrast-to-noise ratio metric, for grid lines in synthetic image with an increasing noise level, is presented in Table 1. The obtained results highlight the improvement obtained by the phase-based approach when applied on st"  
  — Coherence Enhancing Diffusion Filtering based on the Phase Congruency Tensor (DOI: 10.1109/ISBI.2012.6235519) [central,0v]
- · **PCT vesselness inherits the Hessian-family junction failure: like Hessian-based vesselness it is invalid at branching and end points due to ambiguity of eigenvector directions, so the method does NOT solve the junction/crossing dropout problem that the brief flags as the core failure mode of Frangi/Sato vesselness.**  
  "Please note that Hessian-based vesselness measurement is not valid at the branching and end points due to the ambiguity of the directions of the Hessian eigenvectors. Threferore, the PCT vesselness suffers from these issues too."  
  — Coherence Enhancing Diffusion Filtering based on the Phase Congruency Tensor (DOI: 10.1109/ISBI.2012.6235519) [central,0v]
- · **The method introduces a Phase Congruency Tensor (PCT) whose phase angle sets the diffusion direction and whose eigenvalue-based vesselness controls diffusivity; it is a contrast-independent anisotropic diffusion ENHANCEMENT filter, not a binary segmenter — directly relevant as a phase-based preprocessing alternative to the PhenoTypic baseline's structure/intensity assumptions.**  
  "Here we propose a contrast independent anisotropic diffusion filtering of curvilinear structures based on a novel concept: the Phase Congruency Tensor (PCT). The phase angle of the PCT determines the preferred diffusion direction and the di"  
  — Coherence enhancing diffusion filtering based on the Phase Congruency Tensor (Obara et al., ISBI 2012) (URL: https://markfricker.org/wp-content/uploads/2015/12/obara_et_al-2012-isbi-06235519.pdf) [central,0v]
- · **Because it is built on local phase rather than intensity, the filter is largely insensitive to intensity variations along the curve and successfully enhances structures within noisy/low-contrast regions — the exact low-SNR / intensity-gap regime the brief identifies as a hard regime.**  
  "We show that the proposed method is largely insensitive to intensity variations along the curve, and provides successful enhancement within noisy regions."  
  — Coherence enhancing diffusion filtering based on the Phase Congruency Tensor (Obara et al., ISBI 2012) (URL: https://markfricker.org/wp-content/uploads/2015/12/obara_et_al-2012-isbi-06235519.pdf) [central,0v]
- · **The PCT vesselness inherits the same junction/branch-point failure as Hessian-based vesselness (Frangi/Sato): eigenvector-direction ambiguity makes the measure invalid at branching and end points — confirming the brief's claim that single-dominant-orientation eigenvalue models suppress junctions.**  
  "Please note that Hessian-based vesselness measurement is not valid at the branching and end points due to the ambiguity of the directions of the Hessian eigenvectors. Threferore, the PCT vesselness suffers from these issues too."  
  — Coherence enhancing diffusion filtering based on the Phase Congruency Tensor (Obara et al., ISBI 2012) (URL: https://markfricker.org/wp-content/uploads/2015/12/obara_et_al-2012-isbi-06235519.pdf) [central,0v]
- · **The method was validated specifically on saprotrophic FUNGAL networks (as well as retinal vascular networks from the STARE database), demonstrating direct applicability to mycelial/hyphal branching-network imaging.**  
  "we applied the coherence enhancing method ... to the enhancement of flow-like curvilinear structures in synthetic images (see Figure 1) and real biological images of saprotrophic fungal and retinal vascular networks (see Figures 2 and 3)."  
  — Coherence enhancing diffusion filtering based on the Phase Congruency Tensor (Obara et al., ISBI 2012) (URL: https://markfricker.org/wp-content/uploads/2015/12/obara_et_al-2012-isbi-06235519.pdf) [central,0v]
- · **Purple fringing is a chromatic-aberration artifact (Longitudinal Chromatic Aberration / LCA) that appears as colored fringing along high-contrast edges - exactly the confounder that can mimic thin colored filaments in the fungal-hyphae detection brief.**  
  "Purple fringing, a persistent artifact caused by Longitudinal Chromatic Aberration (LCA) in camera lenses, has long degraded the clarity and realism of digital imaging."  
  — DCA-LUT: Deep Chromatic Alignment with 5D LUT for Purple Fringing Removal (URL: https://arxiv.org/html/2511.12066v1) [central,0v]
- · **The method isolates the chromatic-aberration artifact into a dedicated learned channel rather than relying on per-pixel intensity thresholds, demonstrating a learned color-fringe separation approach transferable to cross-channel fringe rejection.**  
  "a dedicated 'purple fringe channel'"  
  — DCA-LUT: Deep Chromatic Alignment with 5D LUT for Purple Fringing Removal (URL: https://arxiv.org/html/2511.12066v1) [central,0v]
- · **Coherence-enhancing diffusion (CED) is an anisotropic nonlinear diffusion process whose diffusion tensor is steered by the structure tensor (second-moment matrix of the smoothed gradient), so that smoothing is directed ALONG the locally dominant orientation rather than across it — making it an orientation-aware ridge/flow preprocessor rather than an isotropic smoother.**  
  "Coherence-Enhancing Diffusion Filtering (Weickert, IJCV 1999) defines an anisotropic diffusion process whose diffusion tensor shares the eigenvectors of the structure tensor and whose eigenvalues are chosen so that diffusion is strong along"  
  — Coherence-Enhancing Diffusion Filtering (DOI: 10.1023/A:1008009714131) [central,0v]

## Foundation segmenters (SAM/SAM2) & small-data transfer  (4 confirmed / 128 total)

- ✅ **Medical SAM3 is built by fully fine-tuning the SAM3 foundation model on large-scale heterogeneous 2D and 3D medical imaging datasets to enable text-prompt-driven (semantic) segmentation without requiring spatial prompts such as points or boxes.**  
  "a foundation model for universal prompt-driven medical image segmentation, obtained by fully fine-tuning SAM3 on large-scale, heterogeneous 2D and 3D medical imaging datasets"  
  — Medical SAM3: A Foundation Model for Universal Prompt-Driven Medical Image Segmentation (URL: https://arxiv.org/html/2601.10880v1) [central,3v]
- ✅ **Fine-tuning SAM2 by adapting only the mask decoder (no added architectural layers) achieves high segmentation accuracy with small training datasets, supporting a small-data transfer-learning workflow rather than training from scratch.**  
  "By coupling mask-decoder fine-tuning with biologically informed post-processing, our framework achieves robust segmentation across diverse imaging modalities... High segmentation accuracy (Dice/Jaccard scores) achieved with small datasets."  
  — Lightweight open-source fine-tuning of SAM2 enables domain-specific microscopy segmentation (URL: https://www.biorxiv.org/content/10.1101/2025.11.08.687405.full.pdf) [central,3v]
- ✅ **The fine-tuning pipeline runs in a single Google Colab notebook without specialized hardware, lowering the compute barrier (no large GPU cluster required) for domain-specific SAM2 adaptation.**  
  "we introduce a lightweight, open-source Google Colab pipeline that enables efficient fine-tuning of SAM2 on domain-specific datasets without additional architectural layers or specialized hardware"  
  — Lightweight open-source fine-tuning of SAM2 enables domain-specific microscopy segmentation (URL: https://www.biorxiv.org/content/10.1101/2025.11.08.687405.full.pdf) [central,3v]
- ✅ **Fine-tuned SAM2 substantially improves accuracy over zero-shot/basic SAM2 and matches leading purpose-built segmentation tools.**  
  "fine-tuned SAM2 demonstrates substantial gains of accuracy relative to basic SAM2 and matches leading tools"  
  — Lightweight open-source fine-tuning of SAM2 enables domain-specific microscopy segmentation (URL: https://www.biorxiv.org/content/10.1101/2025.11.08.687405.full.pdf) [central,3v]
- · **Mask-decoder fine-tuning of SAM2 (no added architectural layers) reaches high segmentation accuracy on domain-specific microscopy using only small training datasets, directly supporting the brief's small-set-annotation / transfer-fine-tune learning constraint.**  
  "By coupling mask-decoder fine-tuning with biologically informed post-processing, our framework achieves robust segmentation across diverse imaging modalities... High segmentation accuracy (Dice/Jaccard scores) achieved with small datasets."  
  — Lightweight open-source fine-tuning of SAM2 enables domain-specific microscopy segmentation (URL: https://www.biorxiv.org/content/10.1101/2025.11.08.687405v1.full) [central,0v]
- · **Fine-tuned SAM2 generalizes across distinct imaging domains (tissue and cell), evidencing the transferability of a SAM2-based promptable foundation segmenter to new domains such as a macroscopic RGB agar-plate setting.**  
  "Cross-domain generalization across tissue and cell imaging with biologically informed post-processing."  
  — Lightweight open-source fine-tuning of SAM2 enables domain-specific microscopy segmentation (URL: https://www.biorxiv.org/content/10.1101/2025.11.08.687405v1.full) [central,0v]
- · **SAM2 can be adapted to retinal vessel (thin curvilinear structure) segmentation via LoRA fine-tuning that trains fewer than 5% of the model's parameters, freezing the prompt encoder and inserting LoRA into the hiera_l image encoder and mask decoder.**  
  "fewer than 5% of the original trainable parameters ... LoRA was integrated into the 'hiera_l' architecture for both the image encoder and mask decoder ... other components, such as the prompt encoder, remained frozen"  
  — SAM2LoRA: Composite Loss-Guided, Parameter-Efficient Finetuning of SAM2 for Retinal Fundus Segmentation (URL: https://arxiv.org/html/2510.10288v1) [central,0v]
- · **The method trains on small per-dataset annotation sets (687 training / 246 testing samples for blood vessel segmentation), demonstrating SAM2 transfer to curvilinear segmentation without a large labeled corpus.**  
  "687 training samples and 246 testing samples for blood vessel segmentation"  
  — SAM2LoRA: Composite Loss-Guided, Parameter-Efficient Finetuning of SAM2 for Retinal Fundus Segmentation (URL: https://arxiv.org/html/2510.10288v1) [central,0v]
- · **The composite loss combines BCE, Soft Dice, and Focal Tversky losses (equal-weighted) to address foreground/background class imbalance and overlap, though it does NOT use an explicit topology/centerline-aware loss (no clDice or persistent-homology term).**  
  "equal weighted linear combination of SegmentationBCE, Soft Dice loss, and Focal Tversky loss ... addresses class imbalance by assigning greater weight to hard-to-segment regions"  
  — SAM2LoRA: Composite Loss-Guided, Parameter-Efficient Finetuning of SAM2 for Retinal Fundus Segmentation (URL: https://arxiv.org/html/2510.10288v1) [central,0v]
- · **The fine-tuned SAM2 produces a clean binary mask with strong performance on thick vasculature but degrades on the finest branches, indicating the thin/junction regime remains the failure mode for promptable-foundation-segmenter transfer.**  
  "model demonstrates robust performance in regions of thicker vasculature, with some degradation in finer branches"  
  — SAM2LoRA: Composite Loss-Guided, Parameter-Efficient Finetuning of SAM2 for Retinal Fundus Segmentation (URL: https://arxiv.org/html/2510.10288v1) [central,0v]
- · **TPNet is a tubular-aware prompt-tuning framework for vessel segmentation designed for the few-shot / limited-annotation regime, directly supporting the brief's small-data transfer-learning requirement for thin-structure segmentation.**  
  "we propose TPNet, a novel tubular-aware prompt-tuning framework for pulmonary vessel segmentation under few-shot training with limited annotations."  
  — Few-Shot Pulmonary Vessel Segmentation based on Tubular-Aware Prompt-Tuning (TPNet) (URL: https://pubmed.ncbi.nlm.nih.gov/41650434/) [central,0v]
- · **TPNet adapts a frozen pretrained segmentation backbone via tunable encoding/decoding networks that learn tubular structure as transfer-learning priors, i.e. prompt-tuning/transfer rather than training a large network from scratch.**  
  "based on an advanced and frozen segmentation backbone, TPNet proposes tunable encoding and decoding networks that learn tubular structures as transfer learning priors, bridging the gap between the source and target pulmonary vessel domains."  
  — Few-Shot Pulmonary Vessel Segmentation based on Tubular-Aware Prompt-Tuning (TPNet) (URL: https://pubmed.ncbi.nlm.nih.gov/41650434/) [central,0v]
- · **Connectivity/topology is built into the architecture: a Morphology-Driven Region Growing (MDRG) module exploits the tubular connectivity of vessels to capture fine-grained features, addressing the brief's junction/gap connectivity priority.**  
  "the Morphology-Driven Region Growing (MDRG) module is developed to leverage the tubular connectivity of vessels to guide the network in capturing fine-grained features of pulmonary vessels."  
  — Few-Shot Pulmonary Vessel Segmentation based on Tubular-Aware Prompt-Tuning (TPNet) (URL: https://pubmed.ncbi.nlm.nih.gov/41650434/) [central,0v]
- · **VesselSAM adapts the Segment Anything Model (SAM) for thin/tubular vessel-tree segmentation, motivated by SAM's poor out-of-box performance on low-contrast, ambiguous-boundary, small-region structures — directly analogous to faint hyphae on agar.**  
  "SAM excels in natural image segmentation ... recent studies have highlighted its limitations in the medical domain, particularly noting that medical images have low contrast, ambiguous tissue boundaries, and small regions of interest."  
  — VesselSAM: Leveraging SAM for Aortic Vessel Segmentation with LoRA and Atrous Attention (URL: https://arxiv.org/html/2502.18185v1) [central,0v]
- · **LoRA-based fine-tuning lets the SAM backbone be adapted to vessel segmentation by training only 7% of parameters, making it a low-compute, small-data transfer route rather than full retraining.**  
  "AtrousLoRA leverages LoRA's key concept of applying a low-rank constraint to the transformer features, enabling VesselSAM to fine-tune the model efficiently with only 7% of the trainable parameters."  
  — VesselSAM: Leveraging SAM for Aortic Vessel Segmentation with LoRA and Atrous Attention (URL: https://arxiv.org/html/2502.18185v1) [central,0v]
- · **The approach is explicitly positioned for limited-label regimes, eliminating full retraining while retaining accuracy — matching the brief's small-set-annotation / transfer constraint.**  
  "suitable for tasks with limited data, as it eliminates the need for full retraining while maintaining high performance."  
  — VesselSAM: Leveraging SAM for Aortic Vessel Segmentation with LoRA and Atrous Attention (URL: https://arxiv.org/html/2502.18185v1) [central,0v]

## Hessian vesselness & junction suppression  (5 confirmed / 215 total)

- ✅ **Hessian/Frangi eigenvalue vesselness loses vessel-like structures at junctions because at intersections both eigenvalues become similarly large, driving the vesselness measure toward zero — the named structural reason naive vesselness suppresses X/T crossings.**  
  "both eigenvalues have similarly large values leading to a vesselness measure close to zero. Thus, vessel-like structures can be lost at junctions and therefore"  
  — The Multiscale Bowler-Hat Transform for Blood Vessel Enhancement in Retinal Images (Sazak, Nelson, Obara) (URL: https://arxiv.org/pdf/1709.05495) [central,3v]
- ✅ **The bowler-hat transform keeps the network connected at junctions: because longer line structuring elements still fit within a junction area, junctions are enhanced as brightly as the vessels joining them, unlike many other enhancement methods.**  
  "a junction should appear bright like those vessels joining that junction, something that many other vessel enhancement methods fail to do. This is due to the ability to fit longer line-based structural elements within the junction area. As "  
  — The Multiscale Bowler-Hat Transform for Blood Vessel Enhancement in Retinal Images (Sazak, Nelson, Obara) (URL: https://arxiv.org/pdf/1709.05495) [central,3v]
- ✅ **Frangi vesselness derives a continuous tubular-structure response from the eigenvalues of the multiscale second-order local image structure (the Hessian), i.e. it is a ridge/tubularity filter driven by local second-derivative geometry rather than an intensity threshold — exactly the curvilinear-detection family the brief seeks as an out-of-family alternative to phase congruency.**  
  "The authors examined "the multiscale second order local structure of an image (Hessian)" to develop a vessel enhancement filter. A vesselness metric was derived from the Hessian's eigenvalues"  
  — Multiscale Vessel Enhancement Filtering (Frangi et al., 1998) (URL: https://link.springer.com/chapter/10.1007/bfb0056195) [central,3v]
- ✅ **Frangi's vesselness filter analyzes the multiscale second-order local structure of an image (the Hessian matrix) specifically to develop a vessel/tubular-structure enhancement filter, rather than relying on intensity thresholding.**  
  "The multiscale second order local structure of an image (Hessian) is examined with the purpose of developing a vessel enhancement filter."  
  — Multiscale Vessel Enhancement Filtering (Frangi et al., 1998) (URL: https://link.springer.com/chapter/10.1007/bfb0056195) [central,3v]
- ✅ **The vesselness measure is derived from all eigenvalues of the Hessian matrix, meaning the discrimination of tubular structures is based on local ridge geometry (eigenvalue relationships) rather than a per-pixel intensity decision.**  
  "A vesselness measure is obtained on the basis of all eigenvalues of the Hessian."  
  — Multiscale Vessel Enhancement Filtering (Frangi et al., 1998) (URL: https://link.springer.com/chapter/10.1007/bfb0056195) [central,3v]
- · **Existing Hessian vesselness enhancement filters (the Frangi lineage) produce non-uniform responses between vessels of different radii and have reduced response specifically at vessel edges, bifurcations/junctions, and pathologies such as aneurysms — i.e. they suppress junctions. This is the failure mode the paper targets.**  
  "existing enhancement filters typically show non-uniform responses between vessels of different radii and have lower response at vessel edges, bifurcations, and pathologies like aneurysms"  
  — Beyond Frangi: an improved multiscale vesselness filter (Jerman et al., SPIE 2015) (URL: https://ui.adsabs.harvard.edu/abs/2015SPIE.9413E..2AJ/abstract) [central,0v]
- · **The proposed filter (Jerman vesselness) is built from a RATIO of multiscale Hessian eigenvalues, rather than Frangi's product/structure-tensor measures, which is the algorithmic change responsible for its improved junction and edge response.**  
  "a novel enhancement filter based on ratio of multiscale Hessian eigenvalues"  
  — Beyond Frangi: an improved multiscale vesselness filter (Jerman et al., SPIE 2015) (URL: https://ui.adsabs.harvard.edu/abs/2015SPIE.9413E..2AJ/abstract) [central,0v]
- · **The eigenvalue-ratio formulation yields a close-to-uniform response across all vascular structures (i.e. scale/radius-invariant response) and accurately enhances the border between vessels and background — addressing the non-uniform-across-scales and edge-dropout weaknesses of Frangi.**  
  "yields a close-to-uniform response in all vascular structures and accurately enhances the border between vascular structures and the background"  
  — Beyond Frangi: an improved multiscale vesselness filter (Jerman et al., SPIE 2015) (URL: https://ui.adsabs.harvard.edu/abs/2015SPIE.9413E..2AJ/abstract) [central,0v]
- · **The Lamy et al. ICPR 2020 survey benchmarks seven vesselness/curvilinear-structure filters in a common framework: Frangi (Antiga variant), Sato, Meijering, OOF (Optimally Oriented Flux), Jerman, Zhang, and RORPO. Most are Hessian-eigenvalue-based; OOF is flux-based and RORPO is morphological (path-operator) — directly covering the classical-CV ridge/tubularity family in the brief.**  
  "Seven vesselness/curvilinear filters are evaluated: 1. Antiga (Frangi) 2. Sato 3. Meijering 4. OOF (using |lambda_1 + lambda_2|) 5. Jerman 6. Zhang 7. RORPO"  
  — Vesselness Filters: A Survey with Benchmarks (Lamy et al., ICPR 2020) (URL: https://hal.science/hal-02544493v2/file/Lamy_ICPR_2020.pdf) [central,0v]
- · **The benchmark evaluates filter performance in three distinct regions of interest, one of which is specifically the vessel bifurcations/junctions — making it a direct measurement instrument for the brief's junction-suppression concern (X/T crossings scoring low under single-dominant-orientation Hessian models).**  
  "Metrics were computed on 3 different regions of interest (mask of an organ, vessels neighbourhood and vessels bifurcations), which directly addresses your interest in bifurcation analysis."  
  — Vesselness Filters: A Survey with Benchmarks (Lamy et al., ICPR 2020) (URL: https://hal.science/hal-02544493v2/file/Lamy_ICPR_2020.pdf) [central,0v]
- · **A follow-on paper building on this benchmark attributes to the Jerman filter a reinforcement of responses at bifurcations — identifying Jerman as a junction-preserving alternative to the classic Frangi vesselness whose eigenvalue model suppresses junctions, the exact failure mode the brief flags.**  
  "the Jerman filter's reinforcement of responses at bifurcations"  
  — Vesselness Filters: A Survey with Benchmarks (Lamy et al., ICPR 2020) (URL: https://hal.science/hal-02544493v2/file/Lamy_ICPR_2020.pdf) [central,0v]
- · **The method is the origin of the 'neuriteness' ridge filter: a Hessian-eigenvalue measure that scores how much a local image region resembles a thin elongated (neurite/curvilinear) structure, i.e. an oriented-ridge detector based on local second-derivative geometry rather than an intensity threshold. This is the out-of-family Hessian alternative to the PhenoTypic phase-congruency baseline.**  
  "The implementation calculates the eigenvalues of the Hessian to compute the similarity of an image region to neurites, according to the method described in [Meijering et al., 2004]."  
  — Design and validation of a tool for neurite tracing and analysis in fluorescence microscopy images (Meijering et al., Cytometry Part A 2004) (DOI: 10.1002/cyto.a.20022) [central,0v]
- · **The neuriteness filter exposes a single shaping/tuning parameter (alpha) that controls which elongated features are selected, satisfying the brief's 'single tunable sensitivity knob plus sensible default' requirement; the canonical default for n-D is -1/(ndim+1) (i.e. -1/3 in 2D).**  
  "alpha ... shaping filter constant, that selects maximally flat elongated features ... If None, the default value is chosen to be the optimal value -1/(ndim+1)."  
  — Design and validation of a tool for neurite tracing and analysis in fluorescence microscopy images (Meijering et al., Cytometry Part A 2004) (DOI: 10.1002/cyto.a.20022) [central,0v]
- · **Meijering's neuriteness was explicitly devised to enhance LOW-CONTRAST and intensity-inhomogeneous filamentous structures, using a MODIFIED Hessian (eigenvalues recombined as a linear combination with a tuning parameter, e.g. lambda' = lambda1 + alpha*lambda2) rather than Frangi/Sato's ratio-of-eigenvalues vesselness — directly relevant to the brief's low-SNR (faint-filament) regime.**  
  "Meijering and colleagues introduce the neuriteness measure to enhance low contrast and highly inhomogeneous neurites in bioimages, using a modified Hessian with a tuning parameter and a different combination of eigenvalues."  
  — Design and validation of a tool for neurite tracing and analysis in fluorescence microscopy images (Meijering et al., Cytometry Part A 2004) (DOI: 10.1002/cyto.a.20022) [central,0v]
- · **The technique is SEMI-AUTOMATIC (interactive, user-seeded tracing) rather than a fully unsupervised producer of a binary pixel mask; the ridge-filter response feeds a minimal-path/tracing step that requires user input, so as a whole tool it does NOT output a training-free binary mask by itself.**  
  "To facilitate this task, we developed a semiautomatic neurite tracing technique."  
  — Design and validation of a tool for neurite tracing and analysis in fluorescence microscopy images (Meijering et al., Cytometry Part A 2004) (DOI: 10.1002/cyto.a.20022) [central,0v]
- · **The method is a 3-D line enhancement filter explicitly designed to discriminate curvilinear (line) structures from other structures (sheet/blob/background), addressing the brief's core requirement that ridge/tubular detection cannot be done by intensity thresholding alone.**  
  "A 3-D line enhancement filter is developed with the aim of discriminating line structures from other structures and recovering line structures of various widths."  
  — Three-dimensional multi-scale line filter for segmentation and visualization of curvilinear structures in medical images (Sato et al., Medical Image Analysis 1998) (DOI: 10.1016/S1361-8415(98)80009-1) [central,0v]

## Morphology, path-openings, tensor voting, curvelets, active contours  (5 confirmed / 180 total)

- ✅ **Path openings are morphological filters that explore all paths from a defined class and filter them with a length criterion, designed specifically to preserve long, thin, and tortuous structures in gray-level images — directly applicable to thin curvilinear hyphae detection.**  
  "preserve long, thin, and tortuous structures in gray level images... These operators explore all paths from a defined class, and filter them with a length criterion."  
  — Parsimonious Path Openings and Closings (Morard et al., IEEE TIP 2014) (URL: https://pubmed.ncbi.nlm.nih.gov/24569442/) [central,1v]
- ✅ **RORPO is a non-linear, non-local mathematical-morphology operator (built on path operators) that explicitly avoids the local-neighborhood analysis used by Hessian-style filters, which it says is poorly adapted to curvilinear anisotropy and causes false detections — positioning it as a genuinely out-of-family alternative to the Hessian/phase-congruency baselines.**  
  "This local analysis is not well adapted to the anisotropy of such structures, and often results in false detections. By using path operators, our method called RORPO tends to avoid these false detections, and better detects curvilinear stru"  
  — RORPO: A morphological framework for curvilinear structure analysis (filtering and segmentation of blood vessels) (URL: https://www.researchgate.net/publication/315838176_RORPO_A_morphological_framework_for_curvilinear_structure_analysis_Application_to_the_filtering_and_segmentation_of_blood_vessels) [central,2v]
- ✅ **Path openings and closings are morphological operators specifically designed to preserve long, thin, and tortuous (curvilinear) structures in grayscale images, matching the ridge/filament-like geometry of hyphae.**  
  "Path openings and closings are morphological tools used to preserve long, thin, and tortuous structures in gray level images."  
  — Parsimonious Path Openings and Closings (Morard et al., IEEE TIP 2014) (URL: https://pubmed.ncbi.nlm.nih.gov/24569442/) [central,3v]
- ✅ **The parsimonious path-opening algorithm has computational complexity that is linear in the number of pixels and independent of the structuring-element/opening length, and runs in streaming mode for both integer and floating-point input.**  
  "Its complexity is linear with respect to the number of pixels, independent of the size of the opening. Furthermore, it is fast for any input data accuracy (integer or floating point) and works in stream."  
  — Parsimonious Path Openings and Closings (Morard et al., IEEE TIP 2014) (URL: https://pubmed.ncbi.nlm.nih.gov/24569442/) [central,3v]
- ✅ **The method is extended to incomplete paths containing gaps, so that noise-corrupted curvilinear structures with signal dropout can be processed with the same approach and complexity — directly relevant to gap-bridging across intensity gaps in dense/overlapping hyphae.**  
  "Parsimonious path openings are also extended to incomplete paths, i.e., paths containing gaps. Noise-corrupted paths can thus be processed with the same approach and complexity."  
  — Parsimonious Path Openings and Closings (Morard et al., IEEE TIP 2014) (URL: https://pubmed.ncbi.nlm.nih.gov/24569442/) [central,3v]
- · **Path openings/closings are morphological operators built from families of thin, oriented structuring elements that are NOT required to be perfectly straight, making them suited to detecting curved/branching curvilinear structures (like hyphae) that exact straight-line structuring elements would miss.**  
  "Path openings and closings are algebraic morphological operators using families of thin and oriented structuring elements that are not necessarily perfectly straight."  
  — Efficient complete and incomplete path openings and closings (Talbot & Appleton, Image and Vision Computing) (URL: https://www.sciencedirect.com/science/article/abs/pii/S0262885606002575) [central,0v]
- · **Path operators from mathematical morphology are explicitly designed for filtering, enhancement, detection, and segmentation of thin objects, i.e., they directly target the thin/elongated-structure regime central to hyphae masking.**  
  "filters from mathematical morphology suitable for the filtering, enhancement, detection and segmentation of thin objects."  
  — Efficient complete and incomplete path openings and closings (Talbot & Appleton, Image and Vision Computing) (URL: https://www.sciencedirect.com/science/article/abs/pii/S0262885606002575) [central,0v]
- · **The 'incomplete' (parsimonious) path-opening variant tolerates gaps/interruptions along a line, addressing fragmented or noise-broken curvilinear structures while the implementation remains significantly faster than line-based approaches.**  
  "The implementation remains significantly faster than traditional line-based approaches, addressing issues with compact noise interference."  
  — Efficient complete and incomplete path openings and closings (Talbot & Appleton, Image and Vision Computing) (URL: https://www.sciencedirect.com/science/article/abs/pii/S0262885606002575) [central,0v]
- · **SOAX extracts filament networks using Multiple Stretching Open Active Contours (SOACs) — deformable parametric curves that are automatically placed along intensity ridges (filament centerlines) and stretch by elongation until reaching a filament tip, making it a model-fitting active-contour approach to curvilinear-structure extraction rather than a per-pixel threshold.**  
  "A SOAC is described as "a parametric curve that 'evolves': it is attracted towards the centerline of a filament, stretches by elongation and stops stretching when its end reaches a filament tip." ... SOACs are "automatically placed along in"  
  — SOAX: A software for quantification of 3D biopolymer networks (Xu et al., Scientific Reports) (URL: https://www.nature.com/articles/srep09081) [central,0v]
- · **SOAX explicitly preserves and resolves network junctions/crossings: it clusters nearby T-junctions into a single junction and reconfigures local SOAC connectivity by cutting and splicing curves so they do not end or bend sharply at junctions — directly addressing the junction-suppression failure of single-orientation Hessian vesselness.**  
  "clustering nearby T-junctions into a single junction followed by configuring the local connectivity of SOACs by cutting and splicing them such that they do not end or bend sharply at junctions"  
  — SOAX: A software for quantification of 3D biopolymer networks (Xu et al., Scientific Reports) (URL: https://www.nature.com/articles/srep09081) [central,0v]
- · **SOAX is robust to noise but is not parameter-free across regimes — its parameters must be retuned for the biopolymer type and image SNR, indicating the operating point is SNR-dependent rather than a single fixed default.**  
  "While the SOAX method is robust against noise, its parameters need to be adjusted depending on the type of biopolymer and the image SNR."  
  — SOAX: A software for quantification of 3D biopolymer networks (Xu et al., Scientific Reports) (URL: https://www.nature.com/articles/srep09081) [central,0v]
- · **SOAX is controllable via two key tunable knobs — a ridge threshold (tau) and a stretch factor (k_str) — where the ridge threshold trades sensitivity against false positives (high values initialize SOACs only on very bright filaments; low values seed on background noise), giving an explicit recall/precision sensitivity dial.**  
  "Two critical parameters: ridge threshold (τ) and stretch factor (kₛₜᵣ). "Large values of the ridge threshold τ will result in SOAC initialization only at very bright filaments, while low values may initialize SOACs on background noise.""  
  — SOAX: A software for quantification of 3D biopolymer networks (Xu et al., Scientific Reports) (URL: https://www.nature.com/articles/srep09081) [central,0v]
- · **RORPO is a discrete, non-linear, non-local (semi-global) morphological operator built on path openings, presented as an alternative to local Hessian-based filters (Frangi/Sato/OOF) for curvilinear-structure detection — directly relevant as an out-of-family, training-free classical-CV candidate to beat the phase-congruency baseline.**  
  "RORPO is built upon the notion of path operator from mathematical morphology and is discrete, non-linear and non-local... RORPO is a semi-global, morphological alternative to local, Hessian-based operators for thin structure filtering."  
  — Curvilinear Structure Analysis by Ranking the Orientation Responses of Path Operators (RORPO) (DOI: 10.1109/tpami.2017.2672972) [central,0v]
- · **On synthetic and real 3D images RORPO outperformed Frangi Vesselness, Optimally Oriented Flux, and Hybrid Diffusion with Continuous Switch, giving up to 8% more true positives and 50% fewer false positives than the next-best method — quantitative evidence it beats Hessian-family filters on accuracy.**  
  "The method was compared with three of the most popular curvilinear structure analysis filters (Frangi Vesselness, Optimally Oriented Flux, and Hybrid Diffusion with Continuous Switch), and provides up to 8 percent more true positive and 50 "  
  — Curvilinear Structure Analysis by Ranking the Orientation Responses of Path Operators (RORPO) (DOI: 10.1109/tpami.2017.2672972) [central,0v]
- · **RORPO suppresses junctions/bifurcations and is prone to false negatives: like Hessian vesselness it disconnects structures at bifurcations (which lack a typical tubular shape) and has a suppressive bias — meaning it does NOT solve the brief's junction/gap-connectivity problem and would still require an explicit gap-bridging/reconnection stage.**  
  "the RORPO filter presents a suppressive behaviour, which can lead to a considerable amount of false negatives... it may also disconnect vessels at bifurcations, which do not exhibit a typical tubular shape structure."  
  — Curvilinear Structure Analysis by Ranking the Orientation Responses of Path Operators (RORPO) (DOI: 10.1109/tpami.2017.2672972) [central,0v]
- · **SOAX uses Stretching Open Active Contours (SOACs) — parametric curves attracted to filament centerlines that integrate local intensity contrast/ridge evidence along a hypothesized line rather than per-pixel thresholding, making it robust to noise and unrelated clutter in live-cell images.**  
  "here we provide an open source program, SOAX, designed to extract the centerlines and junctions of biopolymer networks such as those of actin filaments, microtubules, and fibrin, in the presence of image noise and unrelated structures such "  
  — SOAX: A software for quantification of 3D biopolymer networks (stretching open active contours) (DOI: 10.1038/srep09081) [central,0v]

## Oriented voting, RHT & Radon (low-SNR line integration)  (5 confirmed / 196 total)

- ✅ **The RHT integrates evidence of linear structure along orientations through each pixel: a circular window of diameter D_W rolls across the image, and for each pixel it performs a Hough transform restricted to rho=0, reducing the (rho,theta) space to a 1-D function R(theta,x,y) that quantifies how much coherent linear structure passes through that pixel at each angle theta. This per-pixel oriented accumulation is exactly the 'integrate evidence along a hypothesized line' strategy the brief favors for low-SNR curvilinear detection.**  
  "The RHT mapping is performed on a circular domain, di-ameter DW , centered on each image-space pixel ( x0,y 0) in turn (Figure 2, step 4). Then a Hough transform is performed on this area, limited to rho = 0 (Figure 2, step 5). Thus the rho"  
  — Magnetically Aligned HI Fibers and the Rolling Hough Transform (Clark et al. 2014) (URL: https://arxiv.org/pdf/1312.1338) [central,3v]
- ✅ **The RHT exposes a single tunable sensitivity knob Z (a percentage threshold for how much of a candidate line must contain signal), where Z*D_W pixels must contain signal along a direction for that direction to be recorded; setting Z below 100% deliberately accepts structures that are physically coherent even when not visibly connected — i.e. a controllable recall/precision knob with explicit gap-tolerance.**  
  "All intensity over a set inten-sity threshold Z is stored as R(theta,x 0,y 0)... Z is a percentage. In every direction theta, Z×DW pixels must contain signal in order for the transform to record the data in that direction."  
  — Magnetically Aligned HI Fibers and the Rolling Hough Transform (Clark et al. 2014) (URL: https://arxiv.org/pdf/1312.1338) [central,3v]
- ✅ **RHT preprocessing converts the grayscale image into a binary bitmask before the Hough accumulation, via unsharp masking: convolve with a top-hat smoothing kernel of diameter D_K, subtract the smoothed map from the original (a high-pass / large-scale-structure suppression), then threshold at 0. This D_K-controlled background subtraction is directly analogous to the PhenoTypic baseline's large-sigma Gaussian background subtraction, and yields a binary mask intermediate.**  
  "The image is convolved with a two-dimensional top-hat smoothing kernel of a user-deﬁned diameter, DK (Figure 2, step 1). The smoothed data is then subtracted from the original data (Figure 2, step 2), and the resulting map is thresholded at"  
  — Magnetically Aligned HI Fibers and the Rolling Hough Transform (Clark et al. 2014) (URL: https://arxiv.org/pdf/1312.1338) [central,3v]
- ✅ **The RHT is explicitly designed to detect linear structure independent of overall brightness, making it suited to faint structures near the noise floor whose signal is geometric (coherent linearity) rather than intensity-based; it encodes a per-pixel probability of belonging to a coherent linear structure rather than thresholding intensity.**  
  "The RHT operates on two-dimensional data and is de-signed to be sensitive to linear structure irrespective of the overall brightness of the region... The RHT does not merely identify ﬁbers; it encodes the probability that any given image pi"  
  — Magnetically Aligned HI Fibers and the Rolling Hough Transform (Clark et al. 2014) (URL: https://arxiv.org/pdf/1312.1338) [central,2v]
- ✅ **The Rolling Hough Transform integrates evidence along hypothesized lines by performing a Hough transform on a rolling circular window (diameter DW) centered on each pixel, limited to rho=0, reducing parameter space to a 1D function of orientation R(theta, x0, y0) per pixel; this line-integration approach is what makes it suited to faint, low-contrast linear structure.**  
  "The RHT mapping is performed on a circular domain, diameter DW, centered on each image-space pixel (x0,y0) in turn... Then a Hough transform is performed on this area, limited to rho = 0... Thus the rho-theta space is reduced to a one-dimen"  
  — Magnetically Aligned HI Fibers and the Rolling Hough Transform (Clark et al. 2014) (URL: https://arxiv.org/pdf/1312.1338) [central,3v]
- · **LSD detects locally straight contours (line segments) by region-growing the level-line orientation field — grouping connected pixels that share the same gradient-tangent angle up to a tolerance into 'line support regions' — i.e. it keys on local oriented-gradient geometry rather than an intensity threshold. This is the same oriented-evidence-grouping principle the brief seeks (relevant for ridge/curvilinear detection), though LSD models STRAIGHT segments, not curved hyphae.**  
  "The algorithm starts by computing the level-line angle at each pixel to produce a level-line field, i.e., a unit vector field such that all vectors are tangent to the level line going through their base point. Then, this field is segmented "  
  — LSD: A Fast Line Segment Detector with a False Detection Control (DOI: 10.1109/tpami.2008.300) [central,0v]
- · **LSD provides a principled, single tunable sensitivity knob: each candidate rectangle is validated by its Number of False Alarms NFA = (NM)^(5/2)·γ·B(n,k,p), and a detection is produced iff NFA ≤ ε; ε directly bounds the expected number of false detections and 'can be made as small as desired', satisfying the brief's tunable-knob-plus-default requirement (default ε=1, ~one false alarm per image).**  
  "A threshold ε is selected and when a rectangle has NFA(r,i)≤ ε it is called ε-meaningful rectangle and produces a detection."  
  — LSD: A Fast Line Segment Detector with a False Detection Control (DOI: 10.1109/tpami.2008.300) [central,0v]
- · **The a-contrario / Helmholtz validation gives a proven control on detections-on-noise: the expected number of ε-meaningful rectangles under the noise model H0 is ≤ ε. This is the same a-contrario / FDR-style low-SNR thresholding principle the brief flags from astronomy, applied to oriented-gradient evidence, making it a training-free statistical control for faint structure against a noise floor.**  
  "The theorem states that the average number of ε-meaningful rectangles under the a contrario model H0 is less than ε. Thus, the number of detections on noise is controlled by ε and it can be made as small as desired."  
  — LSD: A Fast Line Segment Detector with a False Detection Control (DOI: 10.1109/tpami.2008.300) [central,0v]
- · **FilDReaMS detects filamentary structures by scanning the image with a rotatable rectangular model bar and measuring, per pixel and per orientation, the fraction of the bar's area covered by signal pixels — a Radon/oriented-template integration that estimates a per-pixel orientation distribution function rather than making per-pixel intensity decisions. This is the same low-SNR line-integration principle relevant to faint hyphae detection.**  
  "The input image is scanned with a rectangular model bar, which makes it possible to uncover structures that can be locally approximated by this bar ... we measure the fraction f_i(ψ_b) of the bar area covered by nonzero pixels"  
  — FilDReaMS: Filament Detection and Reconstruction at Multiple Scales (Carrière et al., A&A 2022) (URL: https://www.aanda.org/articles/aa/full_html/2022/12/aa43506-22/aa43506-22.html) [central,0v]
- · **Unlike the Rolling Hough Transform (which collapses orientation to a single expectation value and thus loses junction information), FilDReaMS resolves crossing/branching filaments by detecting multiple local maxima in the per-pixel orientation distribution, assigning two or more orientations at an intersection — a junction-preserving property directly relevant to the junction-suppression failure of Frangi/Sato vesselness.**  
  "FilDReaMS identifies the local maxima of the measured orientation function...through an iterative process ... FilDReaMS detects two potential filaments with orientation angles ψ₁ and ψ₂"  
  — FilDReaMS: Filament Detection and Reconstruction at Multiple Scales (Carrière et al., A&A 2022) (URL: https://www.aanda.org/articles/aa/full_html/2022/12/aa43506-22/aa43506-22.html) [central,0v]
- · **FilDReaMS produces a clean binary mask: a map in which pixels belonging to all significant model bars are set to 1 and all others to 0, which can then be multiplied with the original image to reconstruct filament intensities — meaning it yields the binary pixel-level mask the brief requires.**  
  "We first produce a binary map C′ in which the model bars associated with all the significant filaments have their pixels set to 1, while all the other pixels are set to 0"  
  — FilDReaMS: Filament Detection and Reconstruction at Multiple Scales (Carrière et al., A&A 2022) (URL: https://www.aanda.org/articles/aa/full_html/2022/12/aa43506-22/aa43506-22.html) [central,0v]
- · **FilDReaMS is fully training-free and unsupervised: significance is established via Monte-Carlo simulations of synthetic ideal filaments (10,000 iterations per bar width) with a 5% false-positive p-value threshold, with no labeled data or machine learning — fitting the no-labels data constraint and providing an a-contrario / FDR-style low-SNR significance threshold.**  
  "This Monte-Carlo iteration is repeated 10000 times (each time with a new random region A_j and a new random orientation of the ideal filament) ... There is a 5% chance of mistakenly rejecting an ideal filament that was, in reality, signific"  
  — FilDReaMS: Filament Detection and Reconstruction at Multiple Scales (Carrière et al., A&A 2022) (URL: https://www.aanda.org/articles/aa/full_html/2022/12/aa43506-22/aa43506-22.html) [central,0v]
- · **The Rolling Hough Transform integrates evidence along candidate orientations within a circular window per pixel: it unsharp-masks the image (top-hat smoothing of diameter D_K, subtract, threshold at 0 to a bitmask), then runs a Hough transform restricted to rho=0 over a window of diameter D_W centered on each pixel, accumulating R(theta) along hypothesized lines. This line-integration design is exactly the low-SNR strategy the brief favors over per-pixel thresholding for faint curvilinear structure.**  
  "The first step is to unsharp mask the image. The image is convolved with a two-dimensional top-hat smoothing kernel of a user-defined diameter, DK. The smoothed data is then subtracted from the original data, and the resulting map is thresh"  
  — Magnetically Aligned H I Fibers and the Rolling Hough Transform (DOI: 10.1088/0004-637X/789/1/82) [central,0v]
- · **The RHT exposes a single dominant sensitivity knob, the intensity threshold Z (a percentage): in each direction theta, Z*D_W pixels must contain signal for that direction to be recorded. This is the tunable recall/precision control the brief requires, with a sensible default the authors set at 70%; lowering Z increases sensitivity to faint/broken structure at the cost of recording more low-intensity background.**  
  "Z is a percentage. In every direction theta, Z x DW pixels must contain signal in order for the transform to record the data in that direction. ... lower intensity thresholds find the same linear features but store too much low-intensity ba"  
  — Magnetically Aligned H I Fibers and the Rolling Hough Transform (DOI: 10.1088/0004-637X/789/1/82) [central,0v]
- · **Setting Z below 100% lets the RHT register a structure as coherent even when it is not visibly/continuously connected, i.e. the method tolerates intensity gaps and dropout along a line - directly relevant to the dense-overlap gap-bridging regime where naive Hessian vesselness fails at junctions.**  
  "Thresholding below 100% (Z < 1) reflects the fact that structures can be physically coherent even if they are not visibly connected."  
  — Magnetically Aligned H I Fibers and the Rolling Hough Transform (DOI: 10.1088/0004-637X/789/1/82) [central,0v]
- · **The statistically optimal method for detecting a thin streak in an astronomical image is matched filtering: cross-correlating the image with a template of a line broadened by the system PSF. This transfers directly to faint-hyphae detection as the theoretically optimal low-SNR ridge detector, integrating evidence along the hypothesized line rather than per-pixel thresholding.**  
  "The optimal method to detect streaks in astronomical images is by cross-correlating the image with a template of a line broadened by the point spread function of the system."  
  — Optimal and Efficient Streak Detection in Astronomical Images (Nir, Ofek et al., AJ 2018) (URL: https://arxiv.org/abs/1806.04204) [central,0v]

## Particle-physics & seismic tracking (gap-bridging)  (4 confirmed / 83 total)

- ✅ **Kalman Filter-based track finding is the dominant, proven approach for connecting detector hits into continuous tracks at the LHC, making it a battle-tested gap-bridging / sequential-hypothesis-extension analog for reconnecting fragmented curvilinear structures.**  
  "The most common track finding techniques in use today...are those based on the Kalman Filter, and are known to provide high physics performance, are robust, and are in use today at the LHC."  
  — Kalman-Filter-Based Particle Tracking on Parallel Architectures at Hadron Colliders (arXiv:1601.08245) (URL: https://arxiv.org/abs/1601.08245) [central,3v]
- ✅ **Track finding/fitting is among the most computationally challenging problems, which motivates re-engineering the Kalman approach for modern parallel hardware -- relevant to whether a Kalman-style reconnection step is affordable in an image pipeline.**  
  "Track finding and fitting is one of the most computationally challenging problems"  
  — Kalman-Filter-Based Particle Tracking on Parallel Architectures at Hadron Colliders (arXiv:1601.08245) (URL: https://arxiv.org/abs/1601.08245) [central,3v]
- ✅ **Ant colony optimization can be applied to seismic coherency data to automatically track faults, improving fault continuity (i.e. bridging discontinuities/gaps in the curvilinear fault network) — directly analogous to gap-bridging reconnection of fragmented thin structures.**  
  "can effectively reduce the noise level and improve the continuity of faults on seismic coherency cube"  
  — Automatic fault tracking based on ant colony algorithms (Computers & Geosciences) (URL: https://www.sciencedirect.com/science/article/abs/pii/S0098300412002804) [central,3v]
- ✅ **The same ant-colony pass that reconnects fault continuity also suppresses noise, i.e. it performs joint denoising and connectivity enhancement on a noisy attribute volume — relevant to the low-SNR + connectivity dual regime in the brief.**  
  "can effectively reduce the noise level"  
  — Automatic fault tracking based on ant colony algorithms (Computers & Geosciences) (URL: https://www.sciencedirect.com/science/article/abs/pii/S0098300412002804) [central,3v]
- · **CATS is a track reconstruction algorithm that combines a cellular automaton for track searching (pattern recognition / linking local hit segments into tracks) with a Kalman filter for track fitting (parameter estimation), establishing the CA+Kalman two-stage architecture as a reference design for reconstructing elongated structures from discrete local evidence — directly analogous to linking ridge fragments into hyphal networks and then estimating their geometry.**  
  "It employs a cellular automaton for track searching and the Kalman filter for track fitting."  
  — CATS: a cellular automaton for tracking in silicon for the HERA-B vertex detector (DOI: 10.1016/S0168-9002(02)00790-8) [central,0v]
- · **The CA-based track searching combined with Kalman fitting yields a very fast algorithm while achieving highly efficient track recognition with accurate, reliable track parameter estimation — evidence that the cellular-automaton connectivity approach is both computationally cheap and high-recall, relevant to the brief's CPU-cost and controllable-recall requirements for a gap-bridging replacement.**  
  "This results in a very fast algorithm that combines highly efficient track recognition with accurate and reliable track parameter estimation."  
  — CATS: a cellular automaton for tracking in silicon for the HERA-B vertex detector (DOI: 10.1016/S0168-9002(02)00790-8) [central,0v]
- · **Cellular-automaton (CA) track finding is a fast, inherently parallelizable alternative to Kalman-filter-based track reconstruction, making it suitable as a replacement/augmentation for sequential cost-based linking such as the PhenoTypic Dijkstra reconnection.**  
  "Track finding methods that are based on cellular automata (CA) offer a fast and parallelizable alternative to the well-established Kalman filter-based algorithms."  
  — Parallel track reconstruction in CMS using the cellular automaton approach (URL: https://www.researchgate.net/publication/263049908_Parallel_track_reconstruction_in_CMS_using_the_cellular_automaton_approach) [central,0v]
- · **Using GPU architectures, the CA approach attains a significant speedup over CPU while still achieving reasonable physics (reconstruction) performance, evidencing a tunable accuracy-vs-compute tradeoff.**  
  "We conclude by evaluating the physics performance, as well as the computational properties of our implementation on various hardware platforms and show that a significant speedup can be attained by using GPU architectures while achieving a "  
  — Parallel track reconstruction in CMS using the cellular automaton approach (URL: https://www.researchgate.net/publication/263049908_Parallel_track_reconstruction_in_CMS_using_the_cellular_automaton_approach) [central,0v]
- · **The paper introduces the Kalman filter as a method for track fitting (estimating a particle trajectory from a sequence of detector hits) and vertex fitting in high-energy physics, providing the foundational recursive-estimation framework later adapted as a 'track finder' that bridges gaps between measurement points along a hypothesized curvilinear path.**  
  "Application of Kalman filtering to track and vertex fitting"  
  — Application of Kalman filtering to track and vertex fitting (DOI: 10.1016/0168-9002(87)90887-4) [central,0v]
- · **The Kalman filter performs recursive, point-by-point estimation: each new measurement updates a running estimate of the track state and its covariance, so the trajectory is built incrementally along its length rather than fit globally in one step — the core mechanism the research brief targets for replacing/augmenting global Dijkstra reconnection with a predict-update tracker.**  
  "Application of Kalman filtering to track and vertex fitting"  
  — Application of Kalman filtering to track and vertex fitting (DOI: 10.1016/0168-9002(87)90887-4) [central,0v]
- · **Ant colony optimization can track curvilinear fault structures by constraining each ant's tracking range using the direction of structure extension and direction consistency, which suppresses noise and makes paths converge along the structure quickly. This is the core gap-bridging / oriented-voting mechanism transferable to reconnecting fragmented hyphae as a replacement for Dijkstra least-cost pathfinding.**  
  "An ant-colony algorithm is a search algorithm based on probability, and after initial positions of the ants are determined, the direction of fault extension and direction consistency are assumed to restrain the ants' tracking range to reduc"  
  — Automatic fault tracking based on ant colony algorithms (Computers & Geosciences) (URL: https://www.sciencedirect.com/science/article/abs/pii/S0098300412002804) [central,0v]
- · **The ant-colony method actively reduces noise and improves continuity of detected linear structures on a noisy attribute volume, addressing the low-SNR regime by exploiting that real structures have better spatial continuity in their extension direction than noise does.**  
  "The ant colony method effectively reduces noise levels and improves fault continuity on seismic coherency cubes."  
  — Automatic fault tracking based on ant colony algorithms (Computers & Geosciences) (URL: https://www.sciencedirect.com/science/article/abs/pii/S0098300412002804) [central,0v]
- · **The pipeline yields a clean discrete/binary linear output: ant-tracked faults are refined by the Zhang-Suen thinning (skeletonization) algorithm to obtain a single-pixel-wide fault line suitable for downstream interpretation. This shows ant-tracking pairs naturally with morphological thinning to produce a binary centerline mask.**  
  "Automatic tracking of faults is refined by Zhang-Suen algorithm to obtain the fault line that can be applied to seismic interpretation."  
  — Automatic fault tracking based on ant colony algorithms (Computers & Geosciences) (URL: https://www.sciencedirect.com/science/article/abs/pii/S0098300412002804) [central,0v]
- · **Ant-tracking (swarm intelligence / ant-colony optimization) is used as the automatic fault-extraction engine that traces and connects amplitude-discontinuity features into continuous fault surfaces in 3D seismic volumes, a direct gap-bridging analog to fragment reconnection.**  
  "the integration and appropriate use of various types of seismic attributes, spectral decomposition, and swarm intelligence"  
  — Enhanced ant tracking: Using a multispectral seismic attribute workflow to improve 3D fault detection (The Leading Edge, SEG) (URL: https://library.seg.org/doi/10.1190/tle40070502.1) [central,0v]
- · **The method is a multi-stage curvilinear-feature workflow: orientation extraction, structure-preserving and frequency filtering, then detection and enhancement of thin discontinuity (ridge-like) features prior to automatic extraction — mirroring an oriented-filter + reconnection pipeline.**  
  "dip-azimuth extraction, structural filtering, frequency filtering, detection of amplitude discontinuities, enhancement of amplitude discontinuities, and automatic fault extraction"  
  — Enhanced ant tracking: Using a multispectral seismic attribute workflow to improve 3D fault detection (The Leading Edge, SEG) (URL: https://library.seg.org/doi/10.1190/tle40070502.1) [central,0v]
- · **Adding a multispectral / spectral-decomposition (multi-attribute) front end measurably improves the extracted ant-tracking result over standard single-input ant-tracking workflows.**  
  "an enhanced ant-tracking volume in which faults are improved compared to common fault-enhanced workflows that incorporate the ant-tracking algorithm"  
  — Enhanced ant tracking: Using a multispectral seismic attribute workflow to improve 3D fault detection (The Leading Edge, SEG) (URL: https://library.seg.org/doi/10.1190/tle40070502.1) [central,0v]

## Phase congruency & optimally oriented flux  (5 confirmed / 204 total)

- ✅ **OOF (Optimally Oriented Flux) localizes its detection at the boundary of a local spherical region and therefore does NOT include the region in the vicinity of the structure, making it robust against disturbance from closely-located adjacent structures — unlike the Hessian, whose second-derivative-of-Gaussian response averages over a neighborhood that can include nearby objects.**  
  "The major advantage of the proposed method is that the OOF based detection is localized at the boundary of the local spherical region. Distinct from the Hessian matrix, OOF does not consider the region in the vicinity of the structure where"  
  — Three Dimensional Curvilinear Structure Detection Using Optimally Oriented Flux (Law & Chung, ECCV 2008) — author PDF (URL: https://cse.hkust.edu.hk/~achung/eccv08_law_chung.pdf) [central,3v]
- ✅ **Hessian-based detection (the Frangi/Sato lineage) degrades specifically when intensity around a structure is non-homogeneous due to closely-located adjacent structures, because the second-derivative-of-Gaussian differential effect is corrupted by neighboring objects — the precise failure mode OOF is designed to avoid (directly relevant to dense/overlapping hyphae causing gaps).**  
  "if the intensity around the objects is not homogeneous due to the presence of closely located adjacent structures, the diffe rential effect given by the second derivatives of Gaussian is adversely affected."  
  — Three Dimensional Curvilinear Structure Detection Using Optimally Oriented Flux (Law & Chung, ECCV 2008) — author PDF (URL: https://cse.hkust.edu.hk/~achung/eccv08_law_chung.pdf) [central,3v]
- ✅ **OOF requires no large-scale Gaussian smoothing (sigma fixed at 1 in all implementations), so it preserves edge sharpness of structure boundaries and is more robust to image noise than the Hessian, whose scale factor must grow for large structures and thereby blurs boundaries that noise then corrupts.**  
  "For OOF, the detection does not require Gaussian smoothing using a large scale factor (σ =1 for OOF). It re-tains the edge sharpness of the structure boundaries. Therefore, the OOF detection has higher robustness against image noise than th"  
  — Three Dimensional Curvilinear Structure Detection Using Optimally Oriented Flux (Law & Chung, ECCV 2008) — author PDF (URL: https://cse.hkust.edu.hk/~achung/eccv08_law_chung.pdf) [central,3v]
- ✅ **On a real angiographic volume, OOF extracted several weak-intensity, low-SNR vessels (arrows 1,2,3,4,7) that the Hessian-based method missed, and OOF resolved the small separation between closely-located vessels that the Hessian merged — empirical evidence for the low-SNR-recovery and gap/junction-preservation advantage over the Hessian/Frangi baseline.**  
  "several vessels with weak intensity (arrows 1, 2, 3, 4 and 7) are missed by the Hessian based method where the OOF based method has no problem to extract them ... the Hessian based method misidentiﬁes closely located vessels as me rged stru"  
  — Three Dimensional Curvilinear Structure Detection Using Optimally Oriented Flux (Law & Chung, ECCV 2008) — author PDF (URL: https://cse.hkust.edu.hk/~achung/eccv08_law_chung.pdf) [central,3v]
- ✅ **OOF localizes its detection at the boundary of a local spherical region rather than integrating over the structure's neighborhood, making it robust to disturbance from closely located adjacent structures (the regime analogous to dense/overlapping hyphae) where Hessian-based methods degrade.**  
  "The major advantage of the proposed method is that the OOF based detection is localized at the boundary of the local spherical region. Distinct from the Hessian matrix, OOF does not consider the region in the vicinity of the structure where"  
  — Three Dimensional Curvilinear Structure Detection Using Optimally Oriented Flux (Law & Chung, ECCV 2008) — author PDF (URL: https://cse.hkust.edu.hk/~achung/eccv08_law_chung.pdf) [central,3v]
- · **Oriented Cylinder Flux (OCF) is an Optimally-Oriented-Flux-derived curvilinear/vessel detector that models vessels as curvilinear cylinders with circular cross-sections, computing cross-section responses from the OOF eigenvector normal spaces and then accumulating those responses along the structure — placing it firmly in the OOF/flux family of training-free ridge detectors and making it an out-of-family alternative to phase-congruency.**  
  "Our method formulates blood vessels as curvilinear cylinders with circular cross-sections, incorporating two-step computations. First, OCF computes cross-section responses based on the normal spaces generated by two eigenvectors in the Opti"  
  — High-Order Oriented Cylindrical Flux for Curvilinear Structure Detection and Vessel Segmentation (MICCAI/IPMI 2019) (URL: https://link.springer.com/chapter/10.1007/978-3-030-20351-1_37) [central,0v]
- · **The method's central innovation is reformulating OCF into a high-order tensor framework on the unit sphere S^3 that can encode multiple orientations within a single voxel — directly targeting the multi-orientation problem at junctions/crossings where single-dominant-orientation Hessian models (Frangi/Sato) suppress X/T crossings.**  
  "We then modify OCF to fit into a high-order tensor framework on a unit sphere S^3, which is able to encode multi-orientation information within a single voxel."  
  — High-Order Oriented Cylindrical Flux for Curvilinear Structure Detection and Vessel Segmentation (MICCAI/IPMI 2019) (URL: https://link.springer.com/chapter/10.1007/978-3-030-20351-1_37) [central,0v]
- · **On synthetic and clinical images the method maintains high segmentation performance across a range of structure radii and across different levels of added random noise, demonstrating strong noise resistance — relevant to the low-SNR regime where evidence must be integrated along the structure.**  
  "the proposed method achieves high segmentation performance under various radii of the curvilinear structures and different levels of random noise, demonstrating that it has a strong noise-resistant ability"  
  — High-Order Oriented Cylindrical Flux for Curvilinear Structure Detection and Vessel Segmentation (MICCAI/IPMI 2019) (URL: https://link.springer.com/chapter/10.1007/978-3-030-20351-1_37) [central,0v]
- · **Monogenic-filter phase congruency runs with lower computational time and smaller memory footprint than log-Gabor (Kovesi-style) phase congruency, making it a cheaper in-family replacement for the PhenoTypic baseline detector.**  
  "the most important advantage of Monogenic filters is that it performs with lower time and smaller memory space compared with Log-Gabor filters"  
  — Image feature detection based on phase congruency by Monogenic filters (URL: https://www.researchgate.net/publication/269295895_Image_feature_detection_based_on_phase_congruency_by_Monogenic_filters) [central,0v]
- · **Monogenic-filter feature detection improves localization accuracy and anti-noise ability relative to log-Gabor phase congruency, with comparable or better overall performance.**  
  "image features with Monogenic filters can not only overcome the limitations of Log-Gabor filters, but also improve the location accuracy and anti-noise ability with comparable or better performance"  
  — Image feature detection based on phase congruency by Monogenic filters (URL: https://www.researchgate.net/publication/269295895_Image_feature_detection_based_on_phase_congruency_by_Monogenic_filters) [central,0v]
- · **The paper proposes a conformal monogenic phase congruency model-based (CMPCM) edge detector that detects edges by locating points of maximum phase congruency in 2D images, giving good spatial-domain analytical capability for local structural features. This is directly in the baseline's phase-congruency family but uses the conformal-monogenic (Riesz-based, rotation-invariant) signal rather than Kovesi's log-Gabor phasecong3, making it a 'better-in-family' candidate.**  
  "The researchers propose a conformal monogenic phase congruency model-based (CMPCM) edge detection algorithm that has good analytical capability in a spatial domain for local structural features and exploits points of maximum phase congruenc"  
  — Conformal monogenic phase congruency model-based edge detection in color images (Multimedia Tools & Applications, 2018) (URL: https://link.springer.com/article/10.1007/s11042-018-6617-x) [central,0v]
- · **CMPCM is designed for and validated on COLOR images, directly addressing the brief's identified chromatic-aberration / color-multichannel gap (the PhenoTypic baseline operates on a single channel); it was tested on natural color images from BSDS500 and LPAICI datasets plus synthetic color images.**  
  "Comprehensive experiments were conducted on synthetic color images and natural color images from BSDS500 and LPAICI standard image datasets."  
  — Conformal monogenic phase congruency model-based edge detection in color images (Multimedia Tools & Applications, 2018) (URL: https://link.springer.com/article/10.1007/s11042-018-6617-x) [central,0v]
- · **CMPCM is reported to outperform Canny, LOG, VPMM, standard phase congruency (PC), and monogenic phase congruency (MPC) on edge-detection quality as measured by Pratt's Figure of Merit, indicating the conformal-monogenic variant beats both classical operators and plain phase congruency.**  
  "The experimental results demonstrated that the proposed CMPCM algorithm outperforms other algorithms such as Canny, LOG, VPMM, PC and MPC"  
  — Conformal monogenic phase congruency model-based edge detection in color images (Multimedia Tools & Applications, 2018) (URL: https://link.springer.com/article/10.1007/s11042-018-6617-x) [central,0v]
- · **Phase congruency is a dimensionless, contrast/illumination-invariant measure of feature significance ranging 0-1, enabling a single universal threshold applied across image classes without per-image empirical tuning - i.e. the 'single tunable sensitivity knob with a sensible default' the brief requires.**  
  "Phase congruency is a dimensionless quantity that is invariant to changes in image brightness or contrast; hence, it provides an absolute measure of the significance of feature points, thus allowing the use of universal threshold values tha"  
  — Image Features from Phase Congruency (DOI: (none)) [central,0v]
- · **A single phase-congruency operator detects ALL feature types - step edges, lines/bars, and roof edges - centered on the line (single response on a line, not the two responses Canny gives), without assuming any luminance profile; this is exactly the ridge/line-detection capability the brief needs (it marks the line itself, not its two edges).**  
  "we are not required to make any assumptions about the luminance profile of the feature; we are simply looking for points where there is order in the frequency domain. Step discontinuities, lines, and roof edges are all detected."  
  — Image Features from Phase Congruency (DOI: (none)) [central,0v]
- · **Low-SNR control is a statistical noise threshold: noise energy follows a Rayleigh distribution whose mean is estimated robustly from the median squared response of the smallest-scale (largest-bandwidth) filter, and a noise circle radius T = mean + k*sigma (k typically 2-3) is soft-thresholded (rectified subtraction) out of local energy before normalization - this IS the baseline's 'energy > noise mean + k sigma' control, sourced here.**  
  "The radius, T , of the noise circle (shown in Figure 4) is taken to be the mean noise response plus some multiple, k, of σR, T = µR + kσR, where k is typically in the range 2 to 3. If we subtract this estimated noise effect from the local e"  
  — Image Features from Phase Congruency (DOI: (none)) [central,0v]

## Remote sensing & materials networks  (3 confirmed / 97 total)

- ✅ **clDice is a training-time loss function (soft-clDice) that is architecture-agnostic — it can be added to any deep-learning segmentation network (U-Net 2D/3D, FCN 2D/3D were tested) to improve topology of thin/tubular structure masks.**  
  "Our soft-clDice loss can be applied to any arbitrary segmentation network ... can be readily deployed to any other deep learning-based segmentation."  
  — clDice - a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR 2021) (URL: https://openaccess.thecvf.com/content/CVPR2021/papers/Shit_clDice_-_A_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_CVPR_2021_paper.pdf) [central,3v]
- ✅ **The soft-skeletonization that makes clDice differentiable is built from iterative min- and max-pooling (the grey-scale analogues of morphological erosion and dilation), so it is fully differentiable and optimizable by gradient descent.**  
  "Min- and max filters are commonly used as the grey-scale alternative of morphological dilation and erosion ... [soft-skeleton uses] iterative min- and max-pooling ... fully differentiable, real-valued, optimizable."  
  — clDice - a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR 2021) (URL: https://openaccess.thecvf.com/content/CVPR2021/papers/Shit_clDice_-_A_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_CVPR_2021_paper.pdf) [central,3v]
- ✅ **Training with clDice recovers true connections that the standard soft-Dice loss drops as false negatives, and avoids the over-segmentation/false-positive connections that soft-Dice produces — directly improving network connectivity. On Massachusetts Roads, Betti-0 error fell from 1.474 to 0.920 and clDice score rose from 70.79% to 76.25%.**  
  "Our networks trained on the proposed loss term recover connections, which were false negatives when trained with the soft-Dice loss."  
  — clDice - a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR 2021) (URL: https://openaccess.thecvf.com/content/CVPR2021/papers/Shit_clDice_-_A_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_CVPR_2021_paper.pdf) [central,1v]
- · **DeepCrack is an end-to-end deep CNN (extended FCN + Deeply-Supervised Nets, VGG-16/SegNet/HED lineage) that produces pixel-wise binary segmentation of thin curvilinear crack structures, achieving mean IoU 85.9, best F-score 86.5, at 0.1 s per image — a directly transferable learned-segmentation template for hyphae/curvilinear masks.**  
  "we propose a deep hierarchical convolutional neural network (CNN), called as DeepCrack, to predict pixel-wise crack segmentation in an end-to-end method... Our method achieved state-of-the-art performances on the proposed dataset (mean I/U "  
  — DeepCrack: A deep hierarchical feature learning architecture for crack segmentation (DOI: 10.1016/j.neucom.2019.01.036) [central,0v]
- · **Severe foreground/background class imbalance (cracks <5% of pixels; 3.54% overall) is handled by a class-balanced weighted cross-entropy that sets the positive (crack) weight to the ratio of negative-to-positive pixel counts (w1 = C0/C1), preventing the simple loss from saturating — a concrete recipe for the thin-structure imbalance problem in the brief.**  
  "Given that over 95% of the ground truth are non-crack, the simple cross-entropy loss can cause training diﬃculties due to the saturation behavior of the activation function. Therefore, we need to weight the loss differently which termed cla"  
  — DeepCrack: A deep hierarchical feature learning architecture for crack segmentation (DOI: 10.1016/j.neucom.2019.01.036) [central,0v]
- · **The final binary mask is obtained by a single fixed global threshold on the 2-class softmax probability map (best thresholds reported as T = 0.75-0.91), giving a single tunable sensitivity knob with a Precision-Recall trade-off curve — matching the brief's operating-point/controllability requirement.**  
  "The output of the softmax layer is a N -channel map of the probabilities where N is the number of classes ( N = 2 in our application). According to the prediction of the softmax layer, we can get a predicted label for each pixel by a ﬁxed t"  
  — DeepCrack: A deep hierarchical feature learning architecture for crack segmentation (DOI: 10.1016/j.neucom.2019.01.036) [central,0v]
- · **DeepCrack explicitly acknowledges that CNNs fail to produce continuous/complete thin curvilinear segments (dashed-line dropout) and that classical post-processing (length constraint, curvature, geometric features) is REQUISITE for connectivity — confirming the brief's thesis that deep nets are weak on the topology/gap-bridging regime and need explicit reconnection.**  
  "Post-processing, such as length constraint, curvature and geometric features etc., which are broadly applied in traditional methods [22,48] , is requisite to obtain continuous and complete thin crack segments. However, it is the weakness of"  
  — DeepCrack: A deep hierarchical feature learning architecture for crack segmentation (DOI: 10.1016/j.neucom.2019.01.036) [central,0v]
- · **RoadCorrector is a three-stage structure-aware pipeline (road segmentation, connectivity refinement, topology correction) explicitly built to fix broken/fragmented linear structures and inaccurate topology in occluded and overlapping regions — the same connectivity-gap problem as dense overlapping hyphae.**  
  "In complex scenes such as tree or shadow occlusion and stacking of multiple roads, existing road extraction methods suffer from generating broken road surfaces, inaccurate topology, and connections."  
  — RoadCorrector: A Structure-Aware Road Extraction Method for Road Connectivity and Topology Correction (URL: https://www.researchgate.net/publication/379205983_RoadCorrector_A_Structure-aware_Road_Extraction_Method_for_Road_Connectivity_and_Topology_Correction) [central,0v]
- · **Gap-bridging / reconnection is performed by a connectivity refinement strategy based on an energy function that fuses the segmentation mask with a centerline-tracking output to restore connectivity in occluded and intersection regions — a direct cross-field analog to (and candidate replacement for) the baseline's Dijkstra least-cost reconnection.**  
  "introduces a connectivity refinement strategy based on energy function to deal with discontinuity in occluded and intersection regions"  
  — RoadCorrector: A Structure-Aware Road Extraction Method for Road Connectivity and Topology Correction (URL: https://www.researchgate.net/publication/379205983_RoadCorrector_A_Structure-aware_Road_Extraction_Method_for_Road_Connectivity_and_Topology_Correction) [central,0v]
- · **The segmentation network (MBRE-Net) is a multibranch design that fuses intersection-detection and frequency-domain information to extract the road mask, i.e. it explicitly models junctions/intersections rather than suppressing them, and uses a frequency-domain branch analogous to phase-congruency log-Gabor decomposition.**  
  "it designs a multibranch road extraction network (MBRE-Net) that combines intersection and frequency domain information to extract road masks"  
  — RoadCorrector: A Structure-Aware Road Extraction Method for Road Connectivity and Topology Correction (URL: https://www.researchgate.net/publication/379205983_RoadCorrector_A_Structure-aware_Road_Extraction_Method_for_Road_Connectivity_and_Topology_Correction) [central,0v]
- · **DeepCrack is explicitly designed for thin curvilinear ('line') structures that, like pavement cracks, exhibit poor continuity (gaps) and low contrast (low SNR) — the exact two hard regimes in the brief — and argues low-level/per-pixel features are inadequate for them, motivating learned high-level features instead.**  
  "Cracks are typical line structures that are of interest in many computer-vision applications. In practice, many cracks, e.g., pavement cracks, show poor continuity and low contrast, which bring great challenges to image-based crack detectio"  
  — DeepCrack: Learning Hierarchical Convolutional Features for Crack Detection (IEEE TIP 2019) (URL: https://ieeexplore.ieee.org/abstract/document/8517148/) [central,0v]
- · **The core mechanism is multi-scale hierarchical convolutional feature fusion on a SegNet encoder-decoder, fusing detail-rich large-scale maps with holistic small-scale maps to capture line structures — the canonical deep-learning thin-structure segmentation architecture (brief section B), and a candidate replacement for the phase-congruency baseline.**  
  "multi-scale deep convolutional features learned at hierarchical convolutional stages are fused together to capture the line structures. More detailed representations are made in larger scale feature maps and more holistic representations ar"  
  — DeepCrack: Learning Hierarchical Convolutional Features for Crack Detection (IEEE TIP 2019) (URL: https://ieeexplore.ieee.org/abstract/document/8517148/) [central,0v]
- · **DeepCrack demonstrates cross-dataset transferability: it is trained on a single dataset (CrackTree260) and evaluated on three different datasets, achieving F-measure over 0.87 on average and beating the prior state of the art — evidence the learned thin-structure detector generalizes across imaging conditions (camera type, illumination).**  
  "We train DeepCrack net on one crack dataset and evaluate it on three others. The experimental results demonstrate that DeepCrack achieves F -measure over 0.87 on the three challenging datasets in average and outperforms the current state-of"  
  — DeepCrack: Learning Hierarchical Convolutional Features for Crack Detection (IEEE TIP 2019) (URL: https://ieeexplore.ieee.org/abstract/document/8517148/) [central,0v]
- · **DeepCrack is an end-to-end deep CNN that produces a pixel-wise BINARY crack mask (0=non-crack, 1=crack) by aggregating multi-scale, multi-level hierarchical features across all convolutional stages (HED/DSN-derived) rather than using only the last layer, exploiting that low-level layers preserve thin-structure boundaries while deeper layers are more noise-robust.**  
  "DeepCrack consists of the extended Fully Convolutional Networks (FCN) and the Deeply-Supervised Nets (DSN). During the training, the elaborately designed model learns and aggregates multi-scale and multi-level features from the low convolut"  
  — DeepCrack: A deep hierarchical feature learning architecture for crack segmentation (DOI: 10.1016/j.neucom.2019.01.036) [central,0v]
- · **DeepCrack handles the severe foreground/background class imbalance of thin curvilinear structures (>95% background) with a class-balanced weighted cross-entropy loss, setting the crack-pixel weight to the ratio of non-crack to crack pixels (w1 = C0/C1) — the single most transferable technique for hyphae detection where filaments occupy a tiny pixel fraction.**  
  "Given that over 95% of the ground truth are non-crack, the simple cross-entropy loss can cause training diﬃculties due to the saturation behavior of the activation function. Therefore, we need to weight the loss differently which termed cla"  
  — DeepCrack: A deep hierarchical feature learning architecture for crack segmentation (DOI: 10.1016/j.neucom.2019.01.036) [central,0v]
- · **A learned CNN explicitly FAILS at the connectivity/topology problem for thin, low-pixel-width, fragmented (dashed-line) structures, and still requires traditional curvilinear post-processing (length/curvature/geometric constraints) to recover continuous, complete segments — directly supporting the brief's thesis that gap-bridging must be handled separately from per-pixel segmentation.**  
  "Post-processing, such as length constraint, curvature and geometric features etc., which are broadly applied in traditional methods [22,48] , is requisite to obtain continuous and complete thin crack segments. However, it is the weakness of"  
  — DeepCrack: A deep hierarchical feature learning architecture for crack segmentation (DOI: 10.1016/j.neucom.2019.01.036) [central,0v]

## SOTA curvilinear benchmarks  (3 confirmed / 96 total)

- ✅ **Betti matching is a topology-aware training loss that matches topological features between predicted and ground-truth segmentations via induced matchings between their persistence barcodes, implicitly accounting for the spatial relationship of features (unlike lifetime-only matching in TopoNet).**  
  "The Betti matching loss builds on techniques from topological data analysis, specifically persistent homology."  
  — Efficient Betti Matching Enables Topology-Aware 3D Segmentation via Persistent Homology (URL: https://arxiv.org/html/2407.04683) [central,3v]
- ✅ **The method computes the supervision target (a 'tubed skeleton' of the ground truth) using simple CPU-based image-processing operations (e.g. scikit-image) during data loading or precomputed, rather than computing a differentiable skeleton on predictions on the GPU, so it adds only minimal GPU memory and training time.**  
  "can be computed with simple CPU-based operations using common image processing frameworks (e.g. scikit-image) as part of data-loading or even be precomputed"  
  — Skeleton Recall Loss for Connectivity Conserving and Resource Efficient Segmentation of Thin Tubular Structures (URL: https://arxiv.org/html/2404.03010v1) [central,3v]
- ✅ **It is the first multi-class-capable topology-aware loss for thin-structure segmentation; clDice ran out of memory on the 13-class TopCoW task on an A100 40GB GPU while Skeleton Recall Loss scaled to all classes.**  
  "clDice Loss rendered it infeasible on all 13 classes as it exceeded the memory capacity of an A100 40GB GPU"  
  — Skeleton Recall Loss for Connectivity Conserving and Resource Efficient Segmentation of Thin Tubular Structures (URL: https://arxiv.org/html/2404.03010v1) [central,3v]
- · **TSNet exploits temporal information across consecutive frames of a coronary angiography sequence specifically to repair vessel discontinuities that single-frame segmentation produces in low-contrast regions, indicating that integrating evidence across frames (rather than per-pixel single-image decisions) improves recovery of faint/broken thin structures.**  
  "The method enhances pixel-wise accuracy while addressing vessel discontinuities in low-contrast regions common in single-frame segmentation."  
  — TSNet: Vessel segmentation with sequential frame temporal information in coronary angiography (DOI: 10.1016/j.compmedimag.2025.102540) [central,0v]
- · **TSNet is a multi-input single-output end-to-end U-shaped network with multiple encoders that simultaneously extract spatial features from several input sequence frames, fusing them via a Temporal and Spatial Attention Unit (TSAU) before a single decoder produces the mask for the target frame; the connectivity gain therefore depends on having a temporal stack rather than a single static image.**  
  "TSNet is a multi-input single-output end-to-end U-shaped network that utilizes multiple encoders for simultaneous extraction of spatial features from input sequence frames. TSNet uses TSAU (Temporal and Spatial Attention Units) for feature "  
  — TSNet: Vessel segmentation with sequential frame temporal information in coronary angiography (DOI: 10.1016/j.compmedimag.2025.102540) [central,0v]
- · **TSNet targets topology preservation of thin tubular vessel networks and reports a high centerline-Dice (clDice 0.9354) alongside Dice 0.8966 and IoU 0.8127, demonstrating that topology/centerline-aware metrics are first-class evaluation targets for thin-structure connectivity (the same clDice family the brief lists as a topology-aware criterion).**  
  "TSNet was validated on the SqCS dataset, achieving a Dice score of 0.8966, Acc of 0.9906, IoU of 0.8127, clDice of 0.9354, VCA of 1.9027, BIOU of 0.3565"  
  — TSNet: Vessel segmentation with sequential frame temporal information in coronary angiography (DOI: 10.1016/j.compmedimag.2025.102540) [central,0v]
- · **clDice is a topology-preserving loss that mathematically guarantees topology preservation up to homotopy equivalence for binary 2D and 3D segmentation, directly addressing the connectivity (gap/junction) problem in tubular structures.**  
  "clDice guarantees topology preservation up to homotopy equivalence for binary 2D and 3D segmentation."  
  — clDice — a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR) (URL: https://www.researchgate.net/publication/355885480_clDice_-_a_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_Segmentation) [central,0v]
- · **The loss is computed on the intersection of the segmentation masks and their morphological skeleta (centerlines), making it a centerline-aware loss that targets connectivity rather than per-pixel overlap.**  
  "on the intersection of the segmentation masks and their (morphological) skeleta"  
  — clDice — a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR) (URL: https://www.researchgate.net/publication/355885480_clDice_-_a_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_Segmentation) [central,0v]
- · **Training with soft-clDice yields more accurate connectivity, higher graph similarity, and better volumetric scores than standard losses, indicating it improves network/topology metrics that downstream length/branching measurements depend on.**  
  "more accurate connectivity information, higher graph similarity, and better volumetric scores"  
  — clDice — a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR) (URL: https://www.researchgate.net/publication/355885480_clDice_-_a_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_Segmentation) [central,0v]
- · **clDice is a differentiable, computationally efficient loss function (soft-clDice) that can be plugged into training of arbitrary neural segmentation networks (e.g. U-Net, FCN) to improve segmentation of tubular/curvilinear structures.**  
  "computationally efficient, differentiable loss function (soft-clDice) for training arbitrary neural segmentation networks"  
  — clDice — a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR) (URL: https://www.researchgate.net/publication/355885480_clDice_-_a_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_Segmentation) [central,0v]
- · **The clDice similarity is computed on the intersection of the segmentation masks with their morphological skeleta (centerlines), which is what makes it sensitive to connectivity rather than per-pixel overlap.**  
  "calculated on the intersection of the segmentation masks and their (morphological) skeleta"  
  — clDice — a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR) (URL: https://www.researchgate.net/publication/355885480_clDice_-_a_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_Segmentation) [central,0v]
- · **clDice guarantees topology preservation up to homotopy equivalence for binary 2D and 3D segmentation, directly targeting the connectivity-over-pixel-accuracy priority for networks where missing a connection alters downstream metrics.**  
  "clDice guarantees topology preservation up to homotopy equivalence for binary 2D and 3D segmentation"  
  — clDice — a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR) (URL: https://www.researchgate.net/publication/355885480_clDice_-_a_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_Segmentation) [central,0v]
- · **Training with soft-clDice recovers connections that were false negatives (gaps) under plain soft-Dice training, improving junction/connectivity metrics (e.g. β1 error 1.455 to 1.389 on DRIVE; Opt-J junction F1 on roads 0.881 to 0.916).**  
  "Our networks trained on the proposed loss term recover connections, which were false negatives when trained with the soft-Dice loss"  
  — clDice — a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR) (URL: https://www.researchgate.net/publication/355885480_clDice_-_a_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_Segmentation) [central,0v]
- · **Skeleton Recall Loss is a topology-aware loss for thin tubular structures that achieves connectivity preservation while replacing GPU-intensive differentiable skeletonization with cheap CPU operations, reducing computational overhead by more than 90%.**  
  "Skeleton Recall Loss, which effectively addresses these challenges by circumventing intensive GPU-based calculations with inexpensive CPU operations."  
  — Skeleton Recall Loss for Connectivity Conserving and Resource Efficient Segmentation of Thin Tubular Structures (URL: https://arxiv.org/html/2404.03010v1) [central,0v]
- · **Skeleton Recall Loss adds only ~8% training time and ~2% VRAM versus clDice's ~88% additional training time and ~52% more VRAM, making it far more resource-efficient than the topology loss the PhenoTypic brief names as a candidate.**  
  "only an additional 8% training time and 2% higher VRAM consumption ... vs clDice's 88% additional training time and 52% more VRAM"  
  — Skeleton Recall Loss for Connectivity Conserving and Resource Efficient Segmentation of Thin Tubular Structures (URL: https://arxiv.org/html/2404.03010v1) [central,0v]
- · **Skeleton Recall Loss consistently surpasses or matches prior thin-structure segmentation losses (including clDice) across five public 2D and 3D datasets spanning natural and medical images, in both binary and multi-class tasks.**  
  "Skeleton Recall Loss consistently surpasses previous thin structure segmentation losses on almost all datasets"  
  — Skeleton Recall Loss for Connectivity Conserving and Resource Efficient Segmentation of Thin Tubular Structures (URL: https://arxiv.org/html/2404.03010v1) [central,0v]

## Steger, steerable & matched filters  (5 confirmed / 194 total)

- ✅ **Steger's detector locates line points via the Hessian: it computes Gaussian-smoothed first/second partial derivatives (rx, ry, rxx, rxy, ryy), takes the eigenvector of the Hessian whose eigenvalue has maximum absolute value as the local line direction n(t), and declares a line point where the second directional derivative perpendicular to the line is large — i.e. detection is governed by oriented ridge second-derivative geometry, not an intensity threshold.**  
  "The direction in which the second directional derivative of z(x, y) takes on its maximum absolute value is used as the direction n(t). This direction can be determined by calculating the eigenvalues and eigenvectors of the Hessian matrix"  
  — An Unbiased Detector of Curvilinear Structures (Steger, IEEE TPAMI 1998) (URL: http://howardzzh.com/research/papers/vision/1998.PAMI.Steger.UnbiasedDetector.pdf) [central,2v]
- ✅ **Steger's detector models a line not as an intensity threshold but as a ridge defined by the first and second directional derivatives, extracting line points where the second directional derivative across the line is maximal; salient lines are selected by the magnitude of this second derivative, giving the ridge-geometry signature the brief calls for rather than a per-pixel intensity test.**  
  "The approach only uses the first and second directional derivatives of an image for the extraction of the line points. No specialized directional filters are needed."  
  — An Unbiased Detector of Curvilinear Structures (Steger, IEEE TPAMI 1998) (URL: http://howardzzh.com/research/papers/vision/1998.PAMI.Steger.UnbiasedDetector.pdf) [central,3v]
- ✅ **Junctions are detected and preserved as explicit network structure: linking follows one branch, and when a candidate point already belongs to another line it is marked as a junction and the line is split there, producing a connected lines-and-junctions graph rather than a fragmented mask.**  
  "If this happens, the point is marked as a junction, and the line that contains the point is split into two lines at the junction point, unless it is the first point of the currently processed line, in which case a line describing a closed l"  
  — An Unbiased Detector of Curvilinear Structures (Steger, IEEE TPAMI 1998) (URL: http://howardzzh.com/research/papers/vision/1998.PAMI.Steger.UnbiasedDetector.pdf) [central,3v]
- ✅ **Line linking uses a hysteresis-style double threshold on the second directional derivative: new lines start only where the response exceeds a user-selectable upper threshold, while points are appended while above a lower threshold, giving a single tunable sensitivity knob to trade recall against precision and to follow faint lines into intersections.**  
  "New lines are created as long as the starting point has a second directional derivative that lies above a certain, user-selectable upper threshold. Points are added to the current line as long as their second directional derivative is great"  
  — An Unbiased Detector of Curvilinear Structures (Steger, IEEE TPAMI 1998) (URL: http://howardzzh.com/research/papers/vision/1998.PAMI.Steger.UnbiasedDetector.pdf) [central,3v]
- ✅ **B-COSFIRE filters achieve orientation selectivity for curvilinear/vessel-like structures by computing the weighted geometric mean of a pool of Difference-of-Gaussians filter responses aligned collinearly, i.e. it integrates oriented evidence along a hypothesized line rather than making per-pixel threshold decisions.**  
  "A B-COSFIRE filter achieves orientation selectivity by computing the weighted geometric mean of the output of a pool of Difference-of-Gaussians filters, whose supports are aligned in a collinear manner."  
  — Trainable COSFIRE filters for vessel delineation with application to retinal images (Azzopardi et al., Medical Image Analysis 2015) (URL: https://pubmed.ncbi.nlm.nih.gov/25240643/) [central,3v]
- · **Steerable filters synthesize a filter response at any arbitrary orientation as a linear combination of a fixed, small set of basis filters, and the filter output as a function of orientation can be determined analytically (in closed form) rather than by re-convolving at each angle.**  
  "The authors present an efficient architecture to synthesize filters of arbitrary orientations from linear combinations of basis filters, allowing one to adaptively steer a filter to any orientation, and to determine analytically the filter "  
  — The Design and Use of Steerable Filters (DOI: 10.1109/34.93808) [central,0v]
- · **For the first derivative of a Gaussian, only two basis filters (the x and y partial derivatives) are required to steer the filter to any orientation theta via the interpolation G_theta = cos(theta) G_x + sin(theta) G_y, giving an oriented ridge/edge detector at any angle from a constant-size basis set.**  
  "G_θ = cos(θ)G_x + sin(θ)G_y ... For the first derivative of a Gaussian, only two basis filters are needed: the partial derivatives with respect to the horizontal (x) and vertical (y) directions."  
  — The Design and Use of Steerable Filters (DOI: 10.1109/34.93808) [central,0v]
- · **The method is completely unsupervised / training-free for vessel detection, combining three complementary classical filters (matched filter, Frangi vesselness, Gabor wavelet) rather than learning from hand-labeled vessel masks; only the combination weights are tuned on a training subset. This directly satisfies the brief's training-free classical-CV baseline requirement.**  
  "The main advantage of the proposed methods is that they are completely unsupervised"  
  — Unsupervised Retinal Vessel Segmentation Using Combined Filters / B-COSFIRE (PMC4769136) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC4769136/) [central,0v]
- · **Combining complementary filters covers different SNR/scale regimes: Gabor wavelet and matched filters enhance thin/small vessels while Frangi is more noise-robust, so the fusion improves detection of thin low-contrast vessels. This is the cross-field analog argument for fusing matched/steerable filters with Hessian vesselness rather than relying on one ridge operator.**  
  "The Wavelet Gabor and matched filters enhance the small vessels better, while Frangi's filter is less sensitive to noise"  
  — Unsupervised Retinal Vessel Segmentation Using Combined Filters / B-COSFIRE (PMC4769136) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC4769136/) [central,0v]
- · **On standard retinal benchmarks the unsupervised fusion reaches accuracy 0.9464 / TPR 0.8644 / AUC 0.9513 on DRIVE and accuracy 0.9532 / TPR 0.8254 / AUC 0.9544 on STARE, ranking close to supervised methods (fifth-best accuracy), establishing a realistic performance ceiling for training-free filter-fusion baselines.**  
  "The proposed approach using median ranking has the fifth best accuracy and a value very close to both"  
  — Unsupervised Retinal Vessel Segmentation Using Combined Filters / B-COSFIRE (PMC4769136) (URL: https://pmc.ncbi.nlm.nih.gov/articles/PMC4769136/) [central,0v]
- · **Standard edge-detection algorithms perform poorly on retinal blood vessels because vessels have poor local contrast, motivating a feature-extraction operator built from the vessel's optical/spatial properties rather than a per-pixel intensity edge/threshold. This is the same failure mode the brief identifies for thin low-contrast hyphae against threshold-based colony detectors.**  
  "Blood vessels usually have poor local contrast, and the application of existing edge detection algorithms yield results which are not satisfactory. An operator for feature extraction based on the optical and spatial properties of objects to"  
  — Detection of blood vessels in retinal images using two-dimensional matched filters (Chaudhuri-lineage matched-filter vessel detection) (DOI: 10.1109/42.34715) [central,0v]
- · **The detector models the vessel cross-section gray-level profile as a Gaussian-shaped curve and uses matched-filter detection to find piecewise-linear vessel segments — i.e. it integrates evidence along a hypothesized oriented line via a template matched to the expected ridge profile, the matched-filter strategy the brief recommends for low-SNR curvilinear structures.**  
  "The gray-level profile of the cross section of a blood vessel is approximated by a Gaussian-shaped curve. The concept of matched filter detection of signals is used to detect piecewise linear segments of blood vessels in these images."  
  — Detection of blood vessels in retinal images using two-dimensional matched filters (Chaudhuri-lineage matched-filter vessel detection) (DOI: 10.1109/42.34715) [central,0v]
- · **Orientation is handled by a discrete bank of rotated templates: twelve different templates spanning all possible vessel directions, with the maximum response across orientations taken per pixel. This is the directional/oriented-filter-bank mechanism, directly comparable to the baseline's 8-orientation log-Gabor phase-congruency bank.**  
  "Twelve different templates that are used to search for vessel segments along all possible directions are constructed."  
  — Detection of blood vessels in retinal images using two-dimensional matched filters (Chaudhuri-lineage matched-filter vessel detection) (DOI: 10.1109/42.34715) [central,0v]
- · **The paper presents a general design method for 2D steerable feature detectors (including edge and ridge detectors) by optimizing a Canny-like criterion, yielding filters with closed-form expressions and a fully 2D (rather than separable/1D-extended) design.**  
  "We propose a general approach for the design of 2D feature detectors from a class of steerable functions based on the optimization of a Canny-like criterion. In contrast with previous computational designs, our approach is truly 2D and prov"  
  — Design of Steerable Filters for Feature Detection Using Canny-Like Criteria (DOI: 10.1109/TPAMI.2004.44) [central,0v]
- · **The resulting steerable detectors achieve better orientation selectivity than classical gradient-based or Hessian-based detectors, which is directly relevant to resolving locally-oriented ridge structures and crossings.**  
  "It also yields operators that have a better orientation selectivity than the classical gradient or Hessian-based detectors."  
  — Design of Steerable Filters for Feature Detection Using Canny-Like Criteria (DOI: 10.1109/TPAMI.2004.44) [central,0v]
- · **The method is explicitly instantiated as both edge and ridge detectors, making it directly applicable to ridge/curvilinear (tubular hypha-like) structure detection rather than only edges.**  
  "We illustrate the method with the design of operators for edge and ridge detection."  
  — Design of Steerable Filters for Feature Detection Using Canny-Like Criteria (DOI: 10.1109/TPAMI.2004.44) [central,0v]

## Topology / centerline-aware losses & tracing nets  (4 confirmed / 152 total)

- ✅ **Skeleton Recall Loss preserves connectivity/topology of thin tubular structures while reducing computational overhead by more than 90% relative to existing topology-aware losses, by replacing intensive GPU-based calculations with inexpensive CPU operations.**  
  "reducing computational overheads by more than 90%... circumventing intensive GPU-based calculations with inexpensive CPU operations"  
  — Skeleton Recall Loss for Connectivity Conserving and Resource Efficient Segmentation of Thin Tubular Structures (URL: https://arxiv.org/pdf/2404.03010) [central,3v]
- ✅ **In direct comparison to clDice (a differentiable-skeletonization topology loss), clDice adds ~88% training time and ~52% VRAM over the plain nnU-Net backbone averaged across 5 datasets, whereas Skeleton Recall Loss adds only ~8% training time and ~2% VRAM.**  
  "For our differentiable skeleton baseline clDice Loss, this leads to approximately 88% additional training time and 52% more VRAM consumption compared to the plain nnUNet backbone when averaged across our 5 datasets (excluding multi-class To"  
  — Skeleton Recall Loss for Connectivity Conserving and Resource Efficient Segmentation of Thin Tubular Structures (URL: https://arxiv.org/pdf/2404.03010) [central,3v]
- ✅ **The loss computes a skeleton of the ground-truth mask, dilates it with a radius-2 diamond kernel to make it tubular, and applies a soft recall loss over that tubular skeleton region to emphasize thin centerline structures.**  
  "Subsequently, we dilate the skeleton with a diamond kernel of radius 2 to make it tubular, thereby enlarging the effective area for loss computation around the otherwise thin, single-pixel-wide skeleton."  
  — Skeleton Recall Loss for Connectivity Conserving and Resource Efficient Segmentation of Thin Tubular Structures (URL: https://arxiv.org/pdf/2404.03010) [central,3v]
- ✅ **Embedding persistence images (a vectorized persistent-homology representation) as a topological feature into a U-Net segmentation network preserves curvilinear/vessel connectivity and reduces fragmentation (vessel breaks), cutting the beta-0 connected-component error on the DRIVE retinal vessel benchmark by ~41% (from 217.2 to 126.8) versus a baseline U-Net trained with cross-entropy.**  
  "incorporation of topological features not only refines the pixel-level accuracy but also robustly preserves vessel connectivity, thereby mitigating issues such as vessel breaks"  
  — Leveraging Persistence Image to Enhance Robustness and Performance in Curvilinear Structure Segmentation (URL: https://arxiv.org/html/2601.18045) [central,3v]
- · **clDice is a connectivity/topology-aware similarity measure computed on the intersection of segmentation masks with their morphological skeleta, and optimizing its soft form yields segmentations with more accurate connectivity, higher graph similarity, and better volumetric scores than soft-Dice alone — directly relevant to prioritizing reconnected, junction-preserving hyphal networks over per-pixel accuracy.**  
  "Training on soft-clDice leads to segmentation with more accurate connectivity information, higher graph similarity, and better volumetric scores."  
  — clDice — a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (DOI: 10.1109/cvpr46437.2021.01629) [central,0v]
- · **soft-clDice is a differentiable, computationally efficient loss that can be dropped into any existing segmentation network, so it is usable as a transfer/fine-tune topology-aware loss without architecture changes — fits the small-data learned-option recommendation.**  
  "Our proposed clDice loss can be applied to any arbitrary segmentation network."  
  — clDice — a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (DOI: 10.1109/cvpr46437.2021.01629) [central,0v]
- · **clDice provides a formal topology guarantee (homotopy equivalence) for binary 2D and 3D masks, which is exactly the connectivity/junction-preserving property the brief requires for downstream length/branching metrics.**  
  "clDice guarantees topology preservation up to homotopy equivalence for binary 2D and 3D segmentation."  
  — clDice — a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (DOI: 10.1109/cvpr46437.2021.01629) [central,0v]
- · **Homotopy Warping loss penalizes only topologically critical pixels — found by warping the predicted binary mask toward the GT mask (and vice-versa) via topology-preserving simple-point flips — to fix broken connections in thin curvilinear structures.**  
  "By focusing on these critical pixels, we propose a new homotopy warping loss to train deep image segmentation networks for better topological accuracy."  
  — Structure-Aware Image Segmentation with Homotopy Warping (Hu, NeurIPS 2022) (URL: https://proceedings.neurips.cc/paper_files/paper/2022/file/98143953a7fd1319175b491888fc8df5-Paper-Conference.pdf) [central,0v]
- · **It operates on the binary mask rather than a noisy likelihood map, avoiding TopoNet/persistent-homology's many irrelevant critical points and cubic cost; warping is O(n) via distance transform.**  
  "the method is based on the likelihood map, which can be noisy with a large amount of irrelevant critical points... The computation for persistent homology is cubic to the image size. It is too expensive to recompute at every iteration."  
  — Structure-Aware Image Segmentation with Homotopy Warping (Hu, NeurIPS 2022) (URL: https://proceedings.neurips.cc/paper_files/paper/2022/file/98143953a7fd1319175b491888fc8df5-Paper-Conference.pdf) [central,0v]
- · **On DRIVE retinal vessels, Warping loss beats UNet, TopoNet, clDice and DMT on topology metrics: Betti error 0.812 vs UNet 3.643 and clDice 1.218, with DICE 0.781 vs 0.749.**  
  "DRIVE UNet [39] 0.749 0.834 4.781 ×10−3 3.643 ... clDice [42] 0.753 0.896 4.012 ×10−3 1.218 ... Warping 0.781 0.911 3.419 ×10−3 0.812"  
  — Structure-Aware Image Segmentation with Homotopy Warping (Hu, NeurIPS 2022) (URL: https://proceedings.neurips.cc/paper_files/paper/2022/file/98143953a7fd1319175b491888fc8df5-Paper-Conference.pdf) [central,0v]
- · **Standard deep-learning segmentation losses optimize volumetric overlap at the expense of structural connectivity/topology, which is the failure mode Skeleton Recall Loss targets for thin tubular structures.**  
  "Standard deep learning-based segmentation loss functions...focus on volumetric overlap, often at the expense of preserving structural connectivity or topology."  
  — Skeleton Recall Loss for Connectivity-Conserving and Resource-Efficient Segmentation of Thin Tubular Structures (Kirchhoff et al., ECCV 2024) (URL: https://arxiv.org/abs/2404.03010) [central,0v]
- · **Skeleton Recall Loss reduces computational overhead by more than 90% compared to current topology-preserving losses (e.g. clDice / persistent-homology), making it far cheaper to train.**  
  "substantially reducing computational overheads by more than 90%"  
  — Skeleton Recall Loss for Connectivity-Conserving and Resource-Efficient Segmentation of Thin Tubular Structures (Kirchhoff et al., ECCV 2024) (URL: https://arxiv.org/abs/2404.03010) [central,0v]
- · **The method achieves its efficiency by replacing intensive GPU-based topology computations with inexpensive CPU operations.**  
  "circumventing intensive GPU-based calculations with inexpensive CPU operations"  
  — Skeleton Recall Loss for Connectivity-Conserving and Resource-Efficient Segmentation of Thin Tubular Structures (Kirchhoff et al., ECCV 2024) (URL: https://arxiv.org/abs/2404.03010) [central,0v]
- · **Skeleton Recall Loss outperforms current state-of-the-art topology-preserving methods across five public benchmark datasets for thin-structure / topology-preserving segmentation.**  
  "demonstrates overall superior performance to current state-of-the-art approaches on five public datasets for topology-preserving segmentation"  
  — Skeleton Recall Loss for Connectivity-Conserving and Resource-Efficient Segmentation of Thin Tubular Structures (Kirchhoff et al., ECCV 2024) (URL: https://arxiv.org/abs/2404.03010) [central,0v]
- · **TopoLoss is a continuous-valued, differentiable loss function that enforces the predicted segmentation to have the same topology (same Betti number) as the ground truth, integrable into end-to-end deep network training.**  
  "we design a continuous-valued loss function that enforces a segmentation to have the same topology as the ground truth, i.e., having the same Betti number"  
  — Topology-Preserving Deep Image Segmentation (TopoLoss, Hu et al., NeurIPS 2019) (URL: https://arxiv.org/pdf/1906.05404) [central,0v]
- · **Standard segmentation models systematically make topological errors on fine-scale structures such as broken connections, which is the specific failure mode (gaps in thin/curvilinear structures) the brief flags for dense/overlapping hyphae.**  
  "Segmentation algorithms are prone to make topological errors on fine-scale structures, e.g., broken connections."  
  — Topology-Preserving Deep Image Segmentation (TopoLoss, Hu et al., NeurIPS 2019) (URL: https://arxiv.org/pdf/1906.05404) [central,0v]

## U-Net thin-structure segmentation & class imbalance  (4 confirmed / 185 total)

- ✅ **soft-clDice is a computationally efficient, differentiable loss function that can be used to train arbitrary neural segmentation networks (e.g. any U-Net), directly addressing the brief's topology-aware DL loss for thin tubular structures.**  
  "Extending this, we propose a computationally efficient, differentiable loss function (soft-clDice) for training arbitrary neural segmentation networks."  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR 2021) (URL: https://arxiv.org/abs/2003.07311) [central,3v]
- ✅ **clDice is computed on the intersection of the segmentation masks and their morphological skeleta (centerlines), making it a centerline/skeleton-aware similarity measure rather than a per-pixel overlap measure.**  
  "We introduce a novel similarity measure termed centerlineDice (short clDice), which is calculated on the intersection of the segmentation masks and their (morphological) skeleta."  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR 2021) (URL: https://arxiv.org/abs/2003.07311) [central,3v]
- ✅ **clDice is theoretically proven to guarantee topology preservation up to homotopy equivalence for binary 2D and 3D segmentation, which directly targets connectivity preservation (the junction/gap-bridging requirement in the brief).**  
  "We theoretically prove that clDice guarantees topology preservation up to homotopy equivalence for binary 2D and 3D segmentation."  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR 2021) (URL: https://arxiv.org/abs/2003.07311) [central,3v]
- ✅ **Training with soft-clDice empirically improves connectivity and graph-level network quality (not just pixel overlap), supporting the brief's preference for reconnected, junction-preserving networks for downstream length/branching metrics.**  
  "Training on soft-clDice leads to segmentation with more accurate connectivity information, higher graph similarity, and better volumetric scores."  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR 2021) (URL: https://arxiv.org/abs/2003.07311) [central,3v]
- · **clDice is a topology-aware similarity measure computed on the intersection of segmentation masks with their morphological skeleta; the authors theoretically prove it guarantees topology preservation up to homotopy equivalence for binary 2D and 3D segmentation. This is directly relevant to the brief's requirement for topology-aware losses that preserve hyphal-network connectivity (length/branching metrics depend on it).**  
  "We introduce a novel similarity measure termed centerlineDice (short clDice), which is calculated on the intersection of the segmentation masks and their (morphological) skeleta. We theoretically prove that clDice guarantees topology preser"  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (DOI: 10.48550/arxiv.2003.07311) [central,0v]
- · **The differentiable soft-clDice loss makes skeleton extraction differentiable via iterative min-pooling/max-pooling as a proxy for morphological erosion/dilation, so it can train arbitrary neural segmentation networks (e.g. U-Net) end-to-end. This makes it a drop-in topology-aware loss for the brief's small-data transfer/fine-tune learned option.**  
  "iterative min- and max-pooling is applied as a proxy for morphological erosion and dilation ... we propose a computationally efficient, differentiable loss function (soft-clDice) for training arbitrary neural segmentation networks."  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (DOI: 10.48550/arxiv.2003.07311) [central,0v]
- · **soft-clDice must be combined with soft-Dice via a weighting alpha in [0,0.5] rather than used alone; including soft-clDice in any positive proportion improves topological, volumetric and graph-similarity scores. This is a practical operating-point/controllability note: the alpha blend is the tunable sensitivity knob for the connectivity-vs-pixel-accuracy tradeoff.**  
  "ℒc = (1−α)(1−soft Dice) + α(1−soft clDice) where α∈[0,0.5] ... including soft-clDice in any proportion (α>0) leads to improved topological, volumetric and graph similarity"  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (DOI: 10.48550/arxiv.2003.07311) [central,0v]
- · **Networks trained with the soft-clDice loss recover connections that were false negatives under soft-Dice alone, directly addressing the brief's dense-overlap/intensity-gap regime (recovering missed branches and bridging gaps in the network).**  
  "networks trained on the proposed loss term recover connections, which were false negatives when trained with soft-Dice loss"  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (DOI: 10.48550/arxiv.2003.07311) [central,0v]
- · **The clDice loss improves topological consistency of tubular-structure segmentation only by sacrificing per-pixel overlap accuracy, and it inherits Dice's lack of robustness to noisy/imperfect annotations (relevant to PhenoTypic's no-large-clean-corpus regime).**  
  "being based on the Dice segmentation loss, clDice inherits some of its negative properties, e.g. lack of robustness with respect to noisy annotations. In addition, by design clDice boosts topological consistency at the expense of segmentati"  
  — The Centerline-Cross Entropy Loss for Vessel-Like Structure Segmentation (MICCAI 2024) (URL: https://papers.miccai.org/miccai-2024/paper/1081_paper.pdf) [central,0v]
- · **The proposed clCE loss combines Cross-Entropy's robustness with the centerline/topology focus of clDice, improving both overlap (DSC) and connectivity (clDice metric) simultaneously rather than trading one for the other.**  
  "training with LclCE leads to increases in both DSC and clDice measures everywhere."  
  — The Centerline-Cross Entropy Loss for Vessel-Like Structure Segmentation (MICCAI 2024) (URL: https://papers.miccai.org/miccai-2024/paper/1081_paper.pdf) [central,0v]
- · **On the 3D coronary (ASOCA) dataset, the clDice loss not only lowered segmentation accuracy but failed to improve topological consistency over the Dice baseline, while clCE improved both — indicating clDice can fail outright on complex 3D networks.**  
  "for the most complex scenario, i.e. the 3D segmentation problem on the ASOCA dataset, the LclDice not only attains lower segmentation performance, but it is unable to improve the topological consistency of the baseline LDice. Also in this c"  
  — The Centerline-Cross Entropy Loss for Vessel-Like Structure Segmentation (MICCAI 2024) (URL: https://papers.miccai.org/miccai-2024/paper/1081_paper.pdf) [central,0v]
- · **clDice is a topology-preserving similarity measure computed on the intersection of segmentation masks and their morphological skeleta, designed specifically for tubular/curvilinear structure segmentation.**  
  "a novel similarity measure...calculated on the intersection of the segmentation masks and their (morphological) skeleta."  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR 2021) (URL: https://arxiv.org/abs/2003.07311) [central,0v]
- · **Theoretically, clDice guarantees topology preservation up to homotopy equivalence for binary 2D and 3D segmentation, which is the property that maintains connectivity (no spurious breaks/junctions) in thin networks.**  
  "clDice guarantees topology preservation up to homotopy equivalence for binary 2D and 3D segmentation."  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR 2021) (URL: https://arxiv.org/abs/2003.07311) [central,0v]
- · **The authors provide soft-clDice, a differentiable and computationally efficient loss function that can be plugged into training of arbitrary neural segmentation networks (e.g., U-Net), so it transfers to learned hyphae segmentation as a drop-in loss.**  
  "a computationally efficient, differentiable loss function (soft-clDice) for training arbitrary neural segmentation networks."  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR 2021) (URL: https://arxiv.org/abs/2003.07311) [central,0v]
- · **Training with soft-clDice yields better connectivity and graph-similarity than standard volumetric losses, directly addressing the junction/gap (topology) regime in the brief.**  
  "segmentation with more accurate connectivity information, higher graph similarity, and better volumetric scores."  
  — clDice -- A Novel Topology-Preserving Loss Function for Tubular Structure Segmentation (CVPR 2021) (URL: https://arxiv.org/abs/2003.07311) [central,0v]
- · **Crack pixels are an extreme minority class, and which loss function wins is contingent on the model and dataset, so loss-function choice (not architecture alone) is the lever for thin imbalanced structures.**  
  "Loss functions, which govern a deep learning-based optimization process, have been widely investigated to handle the class imbalanced data issue in crack segmentation. However, their performance varies according to models and datasets, maki"  
  — Crack segmentation of imbalanced data: The role of loss functions (Engineering Structures, ScienceDirect) (URL: https://www.sciencedirect.com/science/article/pii/S0141029623014037) [central,0v]