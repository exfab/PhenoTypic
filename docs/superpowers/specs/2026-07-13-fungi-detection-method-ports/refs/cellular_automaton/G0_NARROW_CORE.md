# A06 narrow-core G0 disposition

The earlier `G0_REPORT.md` correctly blocks the original fungal image-space design. It remains as
the historical record for that broader contract.

The corrected algorithm plan freezes a narrower source-faithful capability named `tricktrack_ca`:
CA evolution and exact-length extraction over a caller-supplied ordered outer-neighbor graph. For
that scope, the selected corpus supplies a compatible license, complete immutable source, runnable
oracle, state dtype, update order, fixed iteration schedule, inclusive root rule, and DFS ordering.
Production is authorized only for this narrow core.

Fungal graph construction, angle and distance semantics, colony ownership, rasterization, detector
integration, exports, and registries remain deferred and are not implied by this disposition.
