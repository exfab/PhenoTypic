"""Recent Runs scanner — sandbox-driven, classifier-typed (Phase 4).

Phase 0 placeholder — implementation lands in Phase 4. See ``GUI_SPEC_V1.md``
section 5.

Per plan: scans sandbox to depth ``--scan-depth``; classifier-driven rows;
status from manifest.
"""
from __future__ import annotations

# TODO(Phase 4): scan_recent_runs(sandbox, depth) -> list[RecentRunRow].

__all__: list[str] = []
