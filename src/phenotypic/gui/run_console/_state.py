"""Per-session UI scratch state (form values, log subscription) (Phase 6).

Phase 0 placeholder — implementation lands in Phase 6. See ``GUI_SPEC_V1.md``
section 5.

Note: this is UI scratch state, not the process-wide ``RunRegistry`` (which
lives in ``shell._runs_registry`` and survives Run console rebuild).
"""
from __future__ import annotations

# TODO(Phase 6): RunConsoleUIState dataclass.

__all__: list[str] = []
