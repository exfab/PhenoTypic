"""Run console Dash factory (Phase 6).

Phase 0 placeholder — implementation lands in Phase 6. See ``GUI_SPEC_V1.md``
section 5 (Run console internals).

Signature mirrors builder + results_viewer:
    ``create_app(sandbox, *, url_prefix="/", max_local_runs=1)``.
"""
from __future__ import annotations

# TODO(Phase 6): create_app(sandbox, *, url_prefix="/", max_local_runs=1).

__all__: list[str] = []
