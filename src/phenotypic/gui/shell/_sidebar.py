"""Sidebar tree component, badges, hidden/symlink toggles (Phase 3).

Phase 0 placeholder — implementation lands in Phase 3. See ``GUI_SPEC_V1.md``
section 3.

Per plan: lazy tree expand (one level per click), capability badges,
hidden/symlinks toggles, Refresh button, ``[↩ from sidebar]`` hand-off store.
"""
from __future__ import annotations

# TODO(Phase 3): sidebar layout + lazy-expand callback + capability badge render.

__all__: list[str] = []
